(function(){"use strict";try{if(typeof document<"u"){var a=document.createElement("style");a.appendChild(document.createTextNode("@keyframes t-spin{0%{transform:rotate(0)}to{transform:rotate(360deg)}}.t-icon{display:inline-block;vertical-align:middle;width:1em;height:1em}.t-icon:before{font-family:unset}.t-icon-loading{animation:t-spin 1s linear infinite}.t-icon.t-size-s,i.t-size-s{font-size:14px}.t-icon.t-size-m,i.t-size-m{font-size:16px}.t-icon.t-size-l,i.t-size-l{font-size:18px}@unocss;.bg-primary-light[data-v-1bc18c22]{background-color:color-mix(in srgb,var(--td-brand-color) 10%,transparent)}.bg-success-light[data-v-1bc18c22]{background-color:color-mix(in srgb,var(--td-success-color) 10%,transparent)}.bg-warning-light[data-v-1bc18c22]{background-color:color-mix(in srgb,var(--td-warning-color) 10%,transparent)}.bg-secondary-light[data-v-1bc18c22]{background-color:color-mix(in srgb,var(--td-bg-color-secondarycontainer) 50%,transparent)}.list-item-anim[data-v-1bc18c22]{animation:slideUp-1bc18c22 .6s cubic-bezier(.2,.8,.2,1) backwards}@keyframes slideUp-1bc18c22{0%{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}.custom-scrollbar[data-v-1bc18c22]::-webkit-scrollbar{width:8px;height:8px}.custom-scrollbar[data-v-1bc18c22]::-webkit-scrollbar-track{background:#0000001a;border-radius:4px}.custom-scrollbar[data-v-1bc18c22]::-webkit-scrollbar-thumb{background:#4a4a4a;border-radius:4px}.custom-scrollbar[data-v-1bc18c22]::-webkit-scrollbar-thumb:hover{background:#666}.relative[data-v-1bc18c22]{position:relative}.grid[data-v-1bc18c22]{display:grid}.grid-cols-1[data-v-1bc18c22]{grid-template-columns:repeat(1,minmax(0,1fr))}.grid-cols-4[data-v-1bc18c22]{grid-template-columns:repeat(4,minmax(0,1fr))}.m-0[data-v-1bc18c22]{margin:0}.mx-auto[data-v-1bc18c22]{margin-left:auto;margin-right:auto}.mb[data-v-1bc18c22],.mb-4[data-v-1bc18c22]{margin-bottom:1rem}.mb-1[data-v-1bc18c22]{margin-bottom:.25rem}.ml-3[data-v-1bc18c22]{margin-left:.75rem}.ml-4[data-v-1bc18c22]{margin-left:1rem}.ml-auto[data-v-1bc18c22]{margin-left:auto}.mt-1[data-v-1bc18c22]{margin-top:.25rem}.mt-2[data-v-1bc18c22]{margin-top:.5rem}.block[data-v-1bc18c22]{display:block}.h-\\[500px\\][data-v-1bc18c22]{height:500px}.h-14[data-v-1bc18c22]{height:3.5rem}.h-2[data-v-1bc18c22]{height:.5rem}.h-2\\.5[data-v-1bc18c22]{height:.625rem}.h-3[data-v-1bc18c22]{height:.75rem}.h-full[data-v-1bc18c22]{height:100%}.w-14[data-v-1bc18c22]{width:3.5rem}.w-2[data-v-1bc18c22]{width:.5rem}.w-2\\.5[data-v-1bc18c22]{width:.625rem}.w-3[data-v-1bc18c22]{width:.75rem}.w-full[data-v-1bc18c22]{width:100%}.flex[data-v-1bc18c22]{display:flex}.flex-1[data-v-1bc18c22]{flex:1 1 0%}.shrink-0[data-v-1bc18c22]{flex-shrink:0}.flex-grow[data-v-1bc18c22]{flex-grow:1}.flex-col[data-v-1bc18c22]{flex-direction:column}.hover\\:-translate-y-1[data-v-1bc18c22]:hover{--un-translate-y:-.25rem;transform:translate(var(--un-translate-x)) translateY(var(--un-translate-y)) translateZ(var(--un-translate-z)) rotate(var(--un-rotate)) rotateX(var(--un-rotate-x)) rotateY(var(--un-rotate-y)) rotate(var(--un-rotate-z)) skew(var(--un-skew-x)) skewY(var(--un-skew-y)) scaleX(var(--un-scale-x)) scaleY(var(--un-scale-y)) scaleZ(var(--un-scale-z))}.transform[data-v-1bc18c22]{transform:translate(var(--un-translate-x)) translateY(var(--un-translate-y)) translateZ(var(--un-translate-z)) rotate(var(--un-rotate)) rotateX(var(--un-rotate-x)) rotateY(var(--un-rotate-y)) rotate(var(--un-rotate-z)) skew(var(--un-skew-x)) skewY(var(--un-skew-y)) scaleX(var(--un-scale-x)) scaleY(var(--un-scale-y)) scaleZ(var(--un-scale-z))}@keyframes pulse-1bc18c22{0%,to{opacity:1}50%{opacity:.5}}.animate-pulse[data-v-1bc18c22]{animation:pulse-1bc18c22 2s cubic-bezier(.4,0,.6,1) infinite}.items-start[data-v-1bc18c22]{align-items:flex-start}.items-center[data-v-1bc18c22]{align-items:center}.justify-center[data-v-1bc18c22]{justify-content:center}.justify-between[data-v-1bc18c22]{justify-content:space-between}.gap-1[data-v-1bc18c22]{gap:.25rem}.gap-1\\.5[data-v-1bc18c22]{gap:.375rem}.gap-2[data-v-1bc18c22]{gap:.5rem}.gap-3[data-v-1bc18c22]{gap:.75rem}.gap-4[data-v-1bc18c22]{gap:1rem}.gap-5[data-v-1bc18c22]{gap:1.25rem}.gap-6[data-v-1bc18c22]{gap:1.5rem}.overflow-hidden[data-v-1bc18c22]{overflow:hidden}.overflow-y-auto[data-v-1bc18c22]{overflow-y:auto}.whitespace-pre-wrap[data-v-1bc18c22]{white-space:pre-wrap}.border[data-v-1bc18c22]{border-width:1px}.border-b[data-v-1bc18c22]{border-bottom-width:1px}.border-\\[\\#404040\\][data-v-1bc18c22]{--un-border-opacity:1;border-color:rgb(64 64 64 / var(--un-border-opacity))}.border-\\[var\\(--td-border-level-2-color\\)\\][data-v-1bc18c22]{border-color:var(--td-border-level-2-color)}.border-\\[var\\(--td-component-border\\)\\][data-v-1bc18c22]{border-color:var(--td-component-border)}.rounded-2xl[data-v-1bc18c22]{border-radius:1rem}.rounded-full[data-v-1bc18c22]{border-radius:9999px}.rounded-xl[data-v-1bc18c22]{border-radius:.75rem}.border-dashed[data-v-1bc18c22]{border-style:dashed}.bg-\\[\\#1e1e1e\\][data-v-1bc18c22]{--un-bg-opacity:1;background-color:rgb(30 30 30 / var(--un-bg-opacity))}.bg-\\[\\#27c93f\\][data-v-1bc18c22]{--un-bg-opacity:1;background-color:rgb(39 201 63 / var(--un-bg-opacity))}.bg-\\[\\#2d2d2d\\][data-v-1bc18c22]{--un-bg-opacity:1;background-color:rgb(45 45 45 / var(--un-bg-opacity))}.bg-\\[\\#ff5f56\\][data-v-1bc18c22]{--un-bg-opacity:1;background-color:rgb(255 95 86 / var(--un-bg-opacity))}.bg-\\[\\#ffbd2e\\][data-v-1bc18c22]{--un-bg-opacity:1;background-color:rgb(255 189 46 / var(--un-bg-opacity))}.bg-\\[var\\(--color-success\\)\\][data-v-1bc18c22]{background-color:var(--color-success)}.bg-\\[var\\(--td-text-color-placeholder\\)\\][data-v-1bc18c22]{background-color:var(--td-text-color-placeholder)}.p-3[data-v-1bc18c22]{padding:.75rem}.p-4[data-v-1bc18c22]{padding:1rem}.p-5[data-v-1bc18c22]{padding:1.25rem}.p-6[data-v-1bc18c22]{padding:1.5rem}.px-4[data-v-1bc18c22]{padding-left:1rem;padding-right:1rem}.py-12[data-v-1bc18c22]{padding-top:3rem;padding-bottom:3rem}.py-2\\.5[data-v-1bc18c22]{padding-top:.625rem;padding-bottom:.625rem}.pb-3[data-v-1bc18c22]{padding-bottom:.75rem}.pt-2[data-v-1bc18c22]{padding-top:.5rem}.pt-4[data-v-1bc18c22]{padding-top:1rem}.text-center[data-v-1bc18c22]{text-align:center}.text-\\[13px\\][data-v-1bc18c22]{font-size:13px}.text-base[data-v-1bc18c22]{font-size:1rem;line-height:1.5rem}.text-sm[data-v-1bc18c22]{font-size:.875rem;line-height:1.25rem}.text-xl[data-v-1bc18c22]{font-size:1.25rem;line-height:1.75rem}.text-xs[data-v-1bc18c22]{font-size:.75rem;line-height:1rem}.text-\\[\\#56b6c2\\][data-v-1bc18c22]{--un-text-opacity:1;color:rgb(86 182 194 / var(--un-text-opacity))}.text-\\[\\#858585\\][data-v-1bc18c22]{--un-text-opacity:1;color:rgb(133 133 133 / var(--un-text-opacity))}.text-\\[\\#d4d4d4\\][data-v-1bc18c22]{--un-text-opacity:1;color:rgb(212 212 212 / var(--un-text-opacity))}.text-\\[var\\(--color-primary\\)\\][data-v-1bc18c22]{color:var(--color-primary)}.text-\\[var\\(--color-success\\)\\][data-v-1bc18c22]{color:var(--color-success)}.text-\\[var\\(--color-warning\\)\\][data-v-1bc18c22]{color:var(--color-warning)}.text-\\[var\\(--td-text-color-placeholder\\)\\][data-v-1bc18c22]{color:var(--td-text-color-placeholder)}.text-\\[var\\(--td-text-color-primary\\)\\][data-v-1bc18c22]{color:var(--td-text-color-primary)}.text-\\[var\\(--td-text-color-secondary\\)\\][data-v-1bc18c22]{color:var(--td-text-color-secondary)}.font-bold[data-v-1bc18c22]{font-weight:700}.font-extrabold[data-v-1bc18c22]{font-weight:800}.leading-relaxed[data-v-1bc18c22]{line-height:1.625}.font-mono[data-v-1bc18c22]{font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,Liberation Mono,Courier New,monospace}.shadow-\\[var\\(--color-success\\)\\][data-v-1bc18c22]{--un-shadow:var(--color-success);box-shadow:var(--un-ring-offset-shadow),var(--un-ring-shadow),var(--un-shadow)}.shadow-inner[data-v-1bc18c22]{--un-shadow:inset 0 2px 4px 0 var(--un-shadow-color, rgb(0 0 0 / .05));box-shadow:var(--un-ring-offset-shadow),var(--un-ring-shadow),var(--un-shadow)}.shadow-sm[data-v-1bc18c22]{--un-shadow:var(--un-shadow-inset) 0 1px 2px 0 var(--un-shadow-color, rgb(0 0 0 / .05));box-shadow:var(--un-ring-offset-shadow),var(--un-ring-shadow),var(--un-shadow)}.hover\\:shadow-md[data-v-1bc18c22]:hover{--un-shadow:var(--un-shadow-inset) 0 4px 6px -1px var(--un-shadow-color, rgb(0 0 0 / .1)),var(--un-shadow-inset) 0 2px 4px -2px var(--un-shadow-color, rgb(0 0 0 / .1));box-shadow:var(--un-ring-offset-shadow),var(--un-ring-shadow),var(--un-shadow)}.light .light-outline[data-v-1bc18c22],.outline[data-v-1bc18c22]{outline-style:solid}.transition-all[data-v-1bc18c22]{transition-property:all;transition-timing-function:cubic-bezier(.4,0,.2,1);transition-duration:.15s}@media(min-width:768px){.md\\:grid-cols-2[data-v-1bc18c22]{grid-template-columns:repeat(2,minmax(0,1fr))}.md\\:flex-row[data-v-1bc18c22]{flex-direction:row}}@media(min-width:1024px){.lg\\:grid-cols-3[data-v-1bc18c22]{grid-template-columns:repeat(3,minmax(0,1fr))}}")),document.head.appendChild(a)}}catch(t){console.error("vite-plugin-css-injected-by-js",t)}})();
function Le(t) {
  return t && t.__esModule && Object.prototype.hasOwnProperty.call(t, "default") ? t.default : t;
}
var J, ie;
function Be() {
  return ie || (ie = 1, J = Vue), J;
}
var n = Be(), G, le;
function He() {
  return le || (le = 1, G = TDesign), G;
}
var $ = He();
function H(t) {
  "@babel/helpers - typeof";
  return H = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(e) {
    return typeof e;
  } : function(e) {
    return e && typeof Symbol == "function" && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : typeof e;
  }, H(t);
}
function Ue(t, e) {
  if (H(t) != "object" || !t) return t;
  var o = t[Symbol.toPrimitive];
  if (o !== void 0) {
    var r = o.call(t, e);
    if (H(r) != "object") return r;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function ze(t) {
  var e = Ue(t, "string");
  return H(e) == "symbol" ? e : e + "";
}
function O(t, e, o) {
  return (e = ze(e)) in t ? Object.defineProperty(t, e, {
    value: o,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : t[e] = o, t;
}
var ae = (t) => {
  var e = ["strokeLinecap", "fillRule", "clipRule", "strokeWidth"];
  return e.includes(t) ? t.replace(/([a-z0-9]|(?=[A-Z]))([A-Z])/g, "$1-$2").toLowerCase() : t;
}, N = (t, e) => {
  var o = {};
  if (t.attrs)
    for (var [r, s] of Object.entries(t.attrs))
      if (typeof s == "string" && s.startsWith("props.")) {
        var i = s.split(".")[1];
        o[ae(r)] = e[i];
      } else
        o[ae(r)] = s;
  t.tag === "svg" && (o.class = e.class, o.style = e.style, o.onClick = e.onClick);
  var l = t.children ? t.children.map((c) => N(c, e)) : [];
  return n.h(t.tag, o, l);
}, Fe = "t", qe = {
  classPrefix: Fe
};
function Ke() {
  var {
    classPrefix: t
  } = qe;
  return {
    SIZE: {
      default: "",
      xs: "".concat(t, "-size-xs"),
      small: "".concat(t, "-size-s"),
      medium: "".concat(t, "-size-m"),
      large: "".concat(t, "-size-l"),
      xl: "".concat(t, "-size-xl"),
      block: "".concat(t, "-size-full-width")
    },
    STATUS: {
      loading: "".concat(t, "-is-loading"),
      disabled: "".concat(t, "-is-disabled"),
      focused: "".concat(t, "-is-focused"),
      success: "".concat(t, "-is-success"),
      error: "".concat(t, "-is-error"),
      warning: "".concat(t, "-is-warning"),
      selected: "".concat(t, "-is-selected"),
      active: "".concat(t, "-is-active"),
      checked: "".concat(t, "-is-checked"),
      current: "".concat(t, "-is-current"),
      hidden: "".concat(t, "-is-hidden"),
      visible: "".concat(t, "-is-visible"),
      expanded: "".concat(t, "-is-expanded"),
      indeterminate: "".concat(t, "-is-indeterminate")
    }
  };
}
function j(t) {
  var e = Ke().SIZE, o = n.computed(() => t.value in e ? e[t.value] : ""), r = n.computed(() => t.value === void 0 || t.value in e ? {} : {
    fontSize: t.value
  });
  return {
    style: r,
    className: o
  };
}
function ce(t, e) {
  var o = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    e && (r = r.filter(function(s) {
      return Object.getOwnPropertyDescriptor(t, s).enumerable;
    })), o.push.apply(o, r);
  }
  return o;
}
function ue(t) {
  for (var e = 1; e < arguments.length; e++) {
    var o = arguments[e] != null ? arguments[e] : {};
    e % 2 ? ce(Object(o), !0).forEach(function(r) {
      O(t, r, o[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : ce(Object(o)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(o, r));
    });
  }
  return t;
}
var Ze = {
  tag: "svg",
  attrs: {
    fill: "none",
    viewBox: "0 0 24 24",
    width: "1em",
    height: "1em"
  },
  children: [{
    tag: "g",
    attrs: {
      id: "add"
    },
    children: [{
      tag: "path",
      attrs: {
        id: "stroke1",
        stroke: "props.strokeColor1",
        d: "M12 5L12 19M19 12L5 12",
        strokeLinecap: "square",
        strokeWidth: "props.strokeWidth"
      }
    }]
  }]
}, Xe = n.defineComponent({
  name: "AddIcon",
  props: {
    size: {
      type: String
    },
    onClick: {
      type: Function
    },
    fillColor: {
      type: [Array, String]
    },
    strokeColor: {
      type: [Array, String]
    },
    strokeWidth: {
      type: Number
    }
  },
  setup(t, e) {
    var {
      attrs: o
    } = e, r = n.computed(() => t.size), s = n.computed(() => t.strokeColor ? Array.isArray(t.strokeColor) ? t.strokeColor[0] : t.strokeColor : "currentColor"), i = n.computed(() => {
      var h;
      return t.strokeColor ? Array.isArray(t.strokeColor) ? (h = t.strokeColor[1]) !== null && h !== void 0 ? h : t.strokeColor[0] : t.strokeColor : "currentColor";
    }), l = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "transparent"), c = n.computed(() => {
      var h;
      return t.fillColor ? Array.isArray(t.fillColor) ? (h = t.fillColor[1]) !== null && h !== void 0 ? h : t.fillColor[0] : t.fillColor : "transparent";
    }), u = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "currentColor"), {
      className: d,
      style: f
    } = j(r), p = n.computed(() => ["t-icon", "t-icon-add", d.value]), w = n.computed(() => ue(ue({
      fill: "none"
    }, f.value), o.style)), P = n.computed(() => ({
      class: p.value,
      style: w.value,
      onClick: (h) => {
        var m;
        return (m = t.onClick) === null || m === void 0 ? void 0 : m.call(t, {
          e: h
        });
      },
      strokeColor1: s.value,
      strokeColor2: i.value,
      fillColor1: l.value,
      fillColor2: c.value,
      strokeWidth: t.strokeWidth || 2,
      filledColor: u.value
    }));
    return () => N(Ze, P.value);
  }
});
function he(t, e) {
  var o = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    e && (r = r.filter(function(s) {
      return Object.getOwnPropertyDescriptor(t, s).enumerable;
    })), o.push.apply(o, r);
  }
  return o;
}
function de(t) {
  for (var e = 1; e < arguments.length; e++) {
    var o = arguments[e] != null ? arguments[e] : {};
    e % 2 ? he(Object(o), !0).forEach(function(r) {
      O(t, r, o[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : he(Object(o)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(o, r));
    });
  }
  return t;
}
var Je = {
  tag: "svg",
  attrs: {
    fill: "none",
    viewBox: "0 0 24 24",
    width: "1em",
    height: "1em"
  },
  children: [{
    tag: "g",
    attrs: {
      id: "delete"
    },
    children: [{
      tag: "path",
      attrs: {
        id: "fill1",
        fill: "props.fillColor1",
        d: "M5 5H19L18.5 22H5.5L5 5Z"
      }
    }, {
      tag: "path",
      attrs: {
        id: "stroke1",
        stroke: "props.strokeColor1",
        d: "M21 5H3M5 5H19L18.5 22H5.5L5 5ZM8.5 2H15.5V5H8.5V2Z",
        strokeLinecap: "square",
        strokeWidth: "props.strokeWidth"
      }
    }, {
      tag: "path",
      attrs: {
        id: "stroke2",
        stroke: "props.strokeColor2",
        d: "M12 9V18",
        strokeLinecap: "square",
        strokeWidth: "props.strokeWidth"
      }
    }]
  }]
}, Ge = n.defineComponent({
  name: "DeleteIcon",
  props: {
    size: {
      type: String
    },
    onClick: {
      type: Function
    },
    fillColor: {
      type: [Array, String]
    },
    strokeColor: {
      type: [Array, String]
    },
    strokeWidth: {
      type: Number
    }
  },
  setup(t, e) {
    var {
      attrs: o
    } = e, r = n.computed(() => t.size), s = n.computed(() => t.strokeColor ? Array.isArray(t.strokeColor) ? t.strokeColor[0] : t.strokeColor : "currentColor"), i = n.computed(() => {
      var h;
      return t.strokeColor ? Array.isArray(t.strokeColor) ? (h = t.strokeColor[1]) !== null && h !== void 0 ? h : t.strokeColor[0] : t.strokeColor : "currentColor";
    }), l = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "transparent"), c = n.computed(() => {
      var h;
      return t.fillColor ? Array.isArray(t.fillColor) ? (h = t.fillColor[1]) !== null && h !== void 0 ? h : t.fillColor[0] : t.fillColor : "transparent";
    }), u = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "currentColor"), {
      className: d,
      style: f
    } = j(r), p = n.computed(() => ["t-icon", "t-icon-delete", d.value]), w = n.computed(() => de(de({
      fill: "none"
    }, f.value), o.style)), P = n.computed(() => ({
      class: p.value,
      style: w.value,
      onClick: (h) => {
        var m;
        return (m = t.onClick) === null || m === void 0 ? void 0 : m.call(t, {
          e: h
        });
      },
      strokeColor1: s.value,
      strokeColor2: i.value,
      fillColor1: l.value,
      fillColor2: c.value,
      strokeWidth: t.strokeWidth || 2,
      filledColor: u.value
    }));
    return () => N(Je, P.value);
  }
});
function fe(t, e) {
  var o = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    e && (r = r.filter(function(s) {
      return Object.getOwnPropertyDescriptor(t, s).enumerable;
    })), o.push.apply(o, r);
  }
  return o;
}
function ge(t) {
  for (var e = 1; e < arguments.length; e++) {
    var o = arguments[e] != null ? arguments[e] : {};
    e % 2 ? fe(Object(o), !0).forEach(function(r) {
      O(t, r, o[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : fe(Object(o)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(o, r));
    });
  }
  return t;
}
var Qe = {
  tag: "svg",
  attrs: {
    fill: "none",
    viewBox: "0 0 24 24",
    width: "1em",
    height: "1em"
  },
  children: [{
    tag: "g",
    attrs: {
      id: "edit"
    },
    children: [{
      tag: "path",
      attrs: {
        id: "fill1",
        fill: "props.fillColor1",
        d: "M14.1048 6.00427L4.78744 15.3216L3.99805 20.0001L8.67652 19.2107L17.9939 9.89336L14.1048 6.00427Z"
      }
    }, {
      tag: "path",
      attrs: {
        id: "fill2",
        fill: "props.fillColor2",
        d: "M17.9936 9.89339L21.0506 6.83635L17.1615 2.94727L14.1045 6.0043L17.9936 9.89339Z",
        fillRule: "evenodd",
        clipRule: "evenodd"
      }
    }, {
      tag: "path",
      attrs: {
        id: "stroke1",
        stroke: "props.strokeColor1",
        d: "M14.1048 6.00427L4.78744 15.3216L3.99805 20.0001L8.67652 19.2107L17.9939 9.89336M14.1048 6.00427L17.9939 9.89336M14.1048 6.00427L17.1615 2.94727L21.0506 6.83635L17.9939 9.89336",
        strokeLinecap: "square",
        strokeWidth: "props.strokeWidth"
      }
    }]
  }]
}, Ye = n.defineComponent({
  name: "EditIcon",
  props: {
    size: {
      type: String
    },
    onClick: {
      type: Function
    },
    fillColor: {
      type: [Array, String]
    },
    strokeColor: {
      type: [Array, String]
    },
    strokeWidth: {
      type: Number
    }
  },
  setup(t, e) {
    var {
      attrs: o
    } = e, r = n.computed(() => t.size), s = n.computed(() => t.strokeColor ? Array.isArray(t.strokeColor) ? t.strokeColor[0] : t.strokeColor : "currentColor"), i = n.computed(() => {
      var h;
      return t.strokeColor ? Array.isArray(t.strokeColor) ? (h = t.strokeColor[1]) !== null && h !== void 0 ? h : t.strokeColor[0] : t.strokeColor : "currentColor";
    }), l = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "transparent"), c = n.computed(() => {
      var h;
      return t.fillColor ? Array.isArray(t.fillColor) ? (h = t.fillColor[1]) !== null && h !== void 0 ? h : t.fillColor[0] : t.fillColor : "transparent";
    }), u = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "currentColor"), {
      className: d,
      style: f
    } = j(r), p = n.computed(() => ["t-icon", "t-icon-edit", d.value]), w = n.computed(() => ge(ge({
      fill: "none"
    }, f.value), o.style)), P = n.computed(() => ({
      class: p.value,
      style: w.value,
      onClick: (h) => {
        var m;
        return (m = t.onClick) === null || m === void 0 ? void 0 : m.call(t, {
          e: h
        });
      },
      strokeColor1: s.value,
      strokeColor2: i.value,
      fillColor1: l.value,
      fillColor2: c.value,
      strokeWidth: t.strokeWidth || 2,
      filledColor: u.value
    }));
    return () => N(Qe, P.value);
  }
});
function pe(t, e) {
  var o = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    e && (r = r.filter(function(s) {
      return Object.getOwnPropertyDescriptor(t, s).enumerable;
    })), o.push.apply(o, r);
  }
  return o;
}
function _e(t) {
  for (var e = 1; e < arguments.length; e++) {
    var o = arguments[e] != null ? arguments[e] : {};
    e % 2 ? pe(Object(o), !0).forEach(function(r) {
      O(t, r, o[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : pe(Object(o)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(o, r));
    });
  }
  return t;
}
var et = {
  tag: "svg",
  attrs: {
    fill: "none",
    viewBox: "0 0 24 24",
    width: "1em",
    height: "1em"
  },
  children: [{
    tag: "g",
    attrs: {
      id: "play-circle"
    },
    children: [{
      tag: "path",
      attrs: {
        fill: "props.filledColor",
        d: "M12 3C7.02944 3 3 7.02944 3 12C3 16.9706 7.02944 21 12 21C16.9706 21 21 16.9706 21 12C21 7.02944 16.9706 3 12 3ZM1 12C1 5.92487 5.92487 1 12 1C18.0751 1 23 5.92487 23 12C23 18.0751 18.0751 23 12 23C5.92487 23 1 18.0751 1 12Z"
      }
    }, {
      tag: "path",
      attrs: {
        fill: "props.filledColor",
        d: "M18.25 12L8.5 17.6292L8.5 6.37085L18.25 12Z"
      }
    }]
  }]
}, tt = n.defineComponent({
  name: "PlayCircleIcon",
  props: {
    size: {
      type: String
    },
    onClick: {
      type: Function
    },
    fillColor: {
      type: [Array, String]
    },
    strokeColor: {
      type: [Array, String]
    },
    strokeWidth: {
      type: Number
    }
  },
  setup(t, e) {
    var {
      attrs: o
    } = e, r = n.computed(() => t.size), s = n.computed(() => t.strokeColor ? Array.isArray(t.strokeColor) ? t.strokeColor[0] : t.strokeColor : "currentColor"), i = n.computed(() => {
      var h;
      return t.strokeColor ? Array.isArray(t.strokeColor) ? (h = t.strokeColor[1]) !== null && h !== void 0 ? h : t.strokeColor[0] : t.strokeColor : "currentColor";
    }), l = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "transparent"), c = n.computed(() => {
      var h;
      return t.fillColor ? Array.isArray(t.fillColor) ? (h = t.fillColor[1]) !== null && h !== void 0 ? h : t.fillColor[0] : t.fillColor : "transparent";
    }), u = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "currentColor"), {
      className: d,
      style: f
    } = j(r), p = n.computed(() => ["t-icon", "t-icon-play-circle", d.value]), w = n.computed(() => _e(_e({
      fill: "none"
    }, f.value), o.style)), P = n.computed(() => ({
      class: p.value,
      style: w.value,
      onClick: (h) => {
        var m;
        return (m = t.onClick) === null || m === void 0 ? void 0 : m.call(t, {
          e: h
        });
      },
      strokeColor1: s.value,
      strokeColor2: i.value,
      fillColor1: l.value,
      fillColor2: c.value,
      strokeWidth: t.strokeWidth || 2,
      filledColor: u.value
    }));
    return () => N(et, P.value);
  }
});
function me(t, e) {
  var o = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    e && (r = r.filter(function(s) {
      return Object.getOwnPropertyDescriptor(t, s).enumerable;
    })), o.push.apply(o, r);
  }
  return o;
}
function ve(t) {
  for (var e = 1; e < arguments.length; e++) {
    var o = arguments[e] != null ? arguments[e] : {};
    e % 2 ? me(Object(o), !0).forEach(function(r) {
      O(t, r, o[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : me(Object(o)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(o, r));
    });
  }
  return t;
}
var ot = {
  tag: "svg",
  attrs: {
    fill: "none",
    viewBox: "0 0 24 24",
    width: "1em",
    height: "1em"
  },
  children: [{
    tag: "g",
    attrs: {
      id: "stop-circle"
    },
    children: [{
      tag: "path",
      attrs: {
        fill: "props.filledColor",
        d: "M12 3C7.02944 3 3 7.02944 3 12C3 16.9706 7.02944 21 12 21C16.9706 21 21 16.9706 21 12C21 7.02944 16.9706 3 12 3ZM1 12C1 5.92487 5.92487 1 12 1C18.0751 1 23 5.92487 23 12C23 18.0751 18.0751 23 12 23C5.92487 23 1 18.0751 1 12Z"
      }
    }, {
      tag: "path",
      attrs: {
        fill: "props.filledColor",
        d: "M8 8H16V16H8V8Z"
      }
    }]
  }]
}, rt = n.defineComponent({
  name: "StopCircleIcon",
  props: {
    size: {
      type: String
    },
    onClick: {
      type: Function
    },
    fillColor: {
      type: [Array, String]
    },
    strokeColor: {
      type: [Array, String]
    },
    strokeWidth: {
      type: Number
    }
  },
  setup(t, e) {
    var {
      attrs: o
    } = e, r = n.computed(() => t.size), s = n.computed(() => t.strokeColor ? Array.isArray(t.strokeColor) ? t.strokeColor[0] : t.strokeColor : "currentColor"), i = n.computed(() => {
      var h;
      return t.strokeColor ? Array.isArray(t.strokeColor) ? (h = t.strokeColor[1]) !== null && h !== void 0 ? h : t.strokeColor[0] : t.strokeColor : "currentColor";
    }), l = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "transparent"), c = n.computed(() => {
      var h;
      return t.fillColor ? Array.isArray(t.fillColor) ? (h = t.fillColor[1]) !== null && h !== void 0 ? h : t.fillColor[0] : t.fillColor : "transparent";
    }), u = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "currentColor"), {
      className: d,
      style: f
    } = j(r), p = n.computed(() => ["t-icon", "t-icon-stop-circle", d.value]), w = n.computed(() => ve(ve({
      fill: "none"
    }, f.value), o.style)), P = n.computed(() => ({
      class: p.value,
      style: w.value,
      onClick: (h) => {
        var m;
        return (m = t.onClick) === null || m === void 0 ? void 0 : m.call(t, {
          e: h
        });
      },
      strokeColor1: s.value,
      strokeColor2: i.value,
      fillColor1: l.value,
      fillColor2: c.value,
      strokeWidth: t.strokeWidth || 2,
      filledColor: u.value
    }));
    return () => N(ot, P.value);
  }
});
function Ce(t, e) {
  var o = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    e && (r = r.filter(function(s) {
      return Object.getOwnPropertyDescriptor(t, s).enumerable;
    })), o.push.apply(o, r);
  }
  return o;
}
function ye(t) {
  for (var e = 1; e < arguments.length; e++) {
    var o = arguments[e] != null ? arguments[e] : {};
    e % 2 ? Ce(Object(o), !0).forEach(function(r) {
      O(t, r, o[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : Ce(Object(o)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(o, r));
    });
  }
  return t;
}
var nt = {
  tag: "svg",
  attrs: {
    fill: "none",
    viewBox: "0 0 24 24",
    width: "1em",
    height: "1em"
  },
  children: [{
    tag: "g",
    attrs: {
      id: "terminal"
    },
    children: [{
      tag: "path",
      attrs: {
        id: "stroke1",
        stroke: "props.strokeColor1",
        d: "M13 19H21M3.5 17L8.5 12L3.5 7",
        strokeLinecap: "square",
        strokeWidth: "props.strokeWidth"
      }
    }]
  }]
}, st = n.defineComponent({
  name: "TerminalIcon",
  props: {
    size: {
      type: String
    },
    onClick: {
      type: Function
    },
    fillColor: {
      type: [Array, String]
    },
    strokeColor: {
      type: [Array, String]
    },
    strokeWidth: {
      type: Number
    }
  },
  setup(t, e) {
    var {
      attrs: o
    } = e, r = n.computed(() => t.size), s = n.computed(() => t.strokeColor ? Array.isArray(t.strokeColor) ? t.strokeColor[0] : t.strokeColor : "currentColor"), i = n.computed(() => {
      var h;
      return t.strokeColor ? Array.isArray(t.strokeColor) ? (h = t.strokeColor[1]) !== null && h !== void 0 ? h : t.strokeColor[0] : t.strokeColor : "currentColor";
    }), l = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "transparent"), c = n.computed(() => {
      var h;
      return t.fillColor ? Array.isArray(t.fillColor) ? (h = t.fillColor[1]) !== null && h !== void 0 ? h : t.fillColor[0] : t.fillColor : "transparent";
    }), u = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "currentColor"), {
      className: d,
      style: f
    } = j(r), p = n.computed(() => ["t-icon", "t-icon-terminal", d.value]), w = n.computed(() => ye(ye({
      fill: "none"
    }, f.value), o.style)), P = n.computed(() => ({
      class: p.value,
      style: w.value,
      onClick: (h) => {
        var m;
        return (m = t.onClick) === null || m === void 0 ? void 0 : m.call(t, {
          e: h
        });
      },
      strokeColor1: s.value,
      strokeColor2: i.value,
      fillColor1: l.value,
      fillColor2: c.value,
      strokeWidth: t.strokeWidth || 2,
      filledColor: u.value
    }));
    return () => N(nt, P.value);
  }
});
function be(t, e) {
  var o = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var r = Object.getOwnPropertySymbols(t);
    e && (r = r.filter(function(s) {
      return Object.getOwnPropertyDescriptor(t, s).enumerable;
    })), o.push.apply(o, r);
  }
  return o;
}
function we(t) {
  for (var e = 1; e < arguments.length; e++) {
    var o = arguments[e] != null ? arguments[e] : {};
    e % 2 ? be(Object(o), !0).forEach(function(r) {
      O(t, r, o[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : be(Object(o)).forEach(function(r) {
      Object.defineProperty(t, r, Object.getOwnPropertyDescriptor(o, r));
    });
  }
  return t;
}
var it = {
  tag: "svg",
  attrs: {
    fill: "none",
    viewBox: "0 0 24 24",
    width: "1em",
    height: "1em"
  },
  children: [{
    tag: "g",
    attrs: {
      id: "wifi"
    },
    children: [{
      tag: "path",
      attrs: {
        id: "stroke1",
        stroke: "props.strokeColor1",
        d: "M2.10059 9.10049C7.56793 3.63315 16.4323 3.63319 21.8996 9.10049M6.69678 13.6967C9.62572 10.7677 14.3745 10.7678 17.3034 13.6967M11.293 18.2929C11.6835 17.9024 12.3167 17.9024 12.7072 18.2929L12.0001 19L11.293 18.2929Z",
        strokeLinecap: "square",
        strokeWidth: "props.strokeWidth"
      }
    }]
  }]
}, lt = n.defineComponent({
  name: "WifiIcon",
  props: {
    size: {
      type: String
    },
    onClick: {
      type: Function
    },
    fillColor: {
      type: [Array, String]
    },
    strokeColor: {
      type: [Array, String]
    },
    strokeWidth: {
      type: Number
    }
  },
  setup(t, e) {
    var {
      attrs: o
    } = e, r = n.computed(() => t.size), s = n.computed(() => t.strokeColor ? Array.isArray(t.strokeColor) ? t.strokeColor[0] : t.strokeColor : "currentColor"), i = n.computed(() => {
      var h;
      return t.strokeColor ? Array.isArray(t.strokeColor) ? (h = t.strokeColor[1]) !== null && h !== void 0 ? h : t.strokeColor[0] : t.strokeColor : "currentColor";
    }), l = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "transparent"), c = n.computed(() => {
      var h;
      return t.fillColor ? Array.isArray(t.fillColor) ? (h = t.fillColor[1]) !== null && h !== void 0 ? h : t.fillColor[0] : t.fillColor : "transparent";
    }), u = n.computed(() => t.fillColor ? Array.isArray(t.fillColor) ? t.fillColor[0] : t.fillColor : "currentColor"), {
      className: d,
      style: f
    } = j(r), p = n.computed(() => ["t-icon", "t-icon-wifi", d.value]), w = n.computed(() => we(we({
      fill: "none"
    }, f.value), o.style)), P = n.computed(() => ({
      class: p.value,
      style: w.value,
      onClick: (h) => {
        var m;
        return (m = t.onClick) === null || m === void 0 ? void 0 : m.call(t, {
          e: h
        });
      },
      strokeColor1: s.value,
      strokeColor2: i.value,
      fillColor1: l.value,
      fillColor2: c.value,
      strokeWidth: t.strokeWidth || 2,
      filledColor: u.value
    }));
    return () => N(it, P.value);
  }
});
class A extends Error {
  /** Constructs a new instance of {@link @microsoft/signalr.HttpError}.
   *
   * @param {string} errorMessage A descriptive error message.
   * @param {number} statusCode The HTTP status code represented by this error.
   */
  constructor(e, o) {
    const r = new.target.prototype;
    super(`${e}: Status code '${o}'`), this.statusCode = o, this.__proto__ = r;
  }
}
class ee extends Error {
  /** Constructs a new instance of {@link @microsoft/signalr.TimeoutError}.
   *
   * @param {string} errorMessage A descriptive error message.
   */
  constructor(e = "A timeout occurred.") {
    const o = new.target.prototype;
    super(e), this.__proto__ = o;
  }
}
class T extends Error {
  /** Constructs a new instance of {@link AbortError}.
   *
   * @param {string} errorMessage A descriptive error message.
   */
  constructor(e = "An abort occurred.") {
    const o = new.target.prototype;
    super(e), this.__proto__ = o;
  }
}
class at extends Error {
  /** Constructs a new instance of {@link @microsoft/signalr.UnsupportedTransportError}.
   *
   * @param {string} message A descriptive error message.
   * @param {HttpTransportType} transport The {@link @microsoft/signalr.HttpTransportType} this error occurred on.
   */
  constructor(e, o) {
    const r = new.target.prototype;
    super(e), this.transport = o, this.errorType = "UnsupportedTransportError", this.__proto__ = r;
  }
}
class ct extends Error {
  /** Constructs a new instance of {@link @microsoft/signalr.DisabledTransportError}.
   *
   * @param {string} message A descriptive error message.
   * @param {HttpTransportType} transport The {@link @microsoft/signalr.HttpTransportType} this error occurred on.
   */
  constructor(e, o) {
    const r = new.target.prototype;
    super(e), this.transport = o, this.errorType = "DisabledTransportError", this.__proto__ = r;
  }
}
class ut extends Error {
  /** Constructs a new instance of {@link @microsoft/signalr.FailedToStartTransportError}.
   *
   * @param {string} message A descriptive error message.
   * @param {HttpTransportType} transport The {@link @microsoft/signalr.HttpTransportType} this error occurred on.
   */
  constructor(e, o) {
    const r = new.target.prototype;
    super(e), this.transport = o, this.errorType = "FailedToStartTransportError", this.__proto__ = r;
  }
}
class ke extends Error {
  /** Constructs a new instance of {@link @microsoft/signalr.FailedToNegotiateWithServerError}.
   *
   * @param {string} message A descriptive error message.
   */
  constructor(e) {
    const o = new.target.prototype;
    super(e), this.errorType = "FailedToNegotiateWithServerError", this.__proto__ = o;
  }
}
class ht extends Error {
  /** Constructs a new instance of {@link @microsoft/signalr.AggregateErrors}.
   *
   * @param {string} message A descriptive error message.
   * @param {Error[]} innerErrors The collection of errors this error is aggregating.
   */
  constructor(e, o) {
    const r = new.target.prototype;
    super(e), this.innerErrors = o, this.__proto__ = r;
  }
}
class Te {
  constructor(e, o, r) {
    this.statusCode = e, this.statusText = o, this.content = r;
  }
}
class K {
  get(e, o) {
    return this.send({
      ...o,
      method: "GET",
      url: e
    });
  }
  post(e, o) {
    return this.send({
      ...o,
      method: "POST",
      url: e
    });
  }
  delete(e, o) {
    return this.send({
      ...o,
      method: "DELETE",
      url: e
    });
  }
  /** Gets all cookies that apply to the specified URL.
   *
   * @param url The URL that the cookies are valid for.
   * @returns {string} A string containing all the key-value cookie pairs for the specified URL.
   */
  // @ts-ignore
  getCookieString(e) {
    return "";
  }
}
var a;
(function(t) {
  t[t.Trace = 0] = "Trace", t[t.Debug = 1] = "Debug", t[t.Information = 2] = "Information", t[t.Warning = 3] = "Warning", t[t.Error = 4] = "Error", t[t.Critical = 5] = "Critical", t[t.None = 6] = "None";
})(a || (a = {}));
class U {
  constructor() {
  }
  /** @inheritDoc */
  // eslint-disable-next-line
  log(e, o) {
  }
}
U.instance = new U();
const dt = "10.0.0";
class k {
  static isRequired(e, o) {
    if (e == null)
      throw new Error(`The '${o}' argument is required.`);
  }
  static isNotEmpty(e, o) {
    if (!e || e.match(/^\s*$/))
      throw new Error(`The '${o}' argument should not be empty.`);
  }
  static isIn(e, o, r) {
    if (!(e in o))
      throw new Error(`Unknown ${r} value: ${e}.`);
  }
}
class b {
  // react-native has a window but no document so we should check both
  static get isBrowser() {
    return !b.isNode && typeof window == "object" && typeof window.document == "object";
  }
  // WebWorkers don't have a window object so the isBrowser check would fail
  static get isWebWorker() {
    return !b.isNode && typeof self == "object" && "importScripts" in self;
  }
  // react-native has a window but no document
  static get isReactNative() {
    return !b.isNode && typeof window == "object" && typeof window.document > "u";
  }
  // Node apps shouldn't have a window object, but WebWorkers don't either
  // so we need to check for both WebWorker and window
  static get isNode() {
    return typeof process < "u" && process.release && process.release.name === "node";
  }
}
function z(t, e) {
  let o = "";
  return R(t) ? (o = `Binary data of length ${t.byteLength}`, e && (o += `. Content: '${ft(t)}'`)) : typeof t == "string" && (o = `String data of length ${t.length}`, e && (o += `. Content: '${t}'`)), o;
}
function ft(t) {
  const e = new Uint8Array(t);
  let o = "";
  return e.forEach((r) => {
    const s = r < 16 ? "0" : "";
    o += `0x${s}${r.toString(16)} `;
  }), o.substring(0, o.length - 1);
}
function R(t) {
  return t && typeof ArrayBuffer < "u" && (t instanceof ArrayBuffer || // Sometimes we get an ArrayBuffer that doesn't satisfy instanceof
  t.constructor && t.constructor.name === "ArrayBuffer");
}
async function $e(t, e, o, r, s, i) {
  const l = {}, [c, u] = V();
  l[c] = u, t.log(a.Trace, `(${e} transport) sending data. ${z(s, i.logMessageContent)}.`);
  const d = R(s) ? "arraybuffer" : "text", f = await o.post(r, {
    content: s,
    headers: { ...l, ...i.headers },
    responseType: d,
    timeout: i.timeout,
    withCredentials: i.withCredentials
  });
  t.log(a.Trace, `(${e} transport) request complete. Response status: ${f.statusCode}.`);
}
function gt(t) {
  return t === void 0 ? new q(a.Information) : t === null ? U.instance : t.log !== void 0 ? t : new q(t);
}
class pt {
  constructor(e, o) {
    this._subject = e, this._observer = o;
  }
  dispose() {
    const e = this._subject.observers.indexOf(this._observer);
    e > -1 && this._subject.observers.splice(e, 1), this._subject.observers.length === 0 && this._subject.cancelCallback && this._subject.cancelCallback().catch((o) => {
    });
  }
}
class q {
  constructor(e) {
    this._minLevel = e, this.out = console;
  }
  log(e, o) {
    if (e >= this._minLevel) {
      const r = `[${(/* @__PURE__ */ new Date()).toISOString()}] ${a[e]}: ${o}`;
      switch (e) {
        case a.Critical:
        case a.Error:
          this.out.error(r);
          break;
        case a.Warning:
          this.out.warn(r);
          break;
        case a.Information:
          this.out.info(r);
          break;
        default:
          this.out.log(r);
          break;
      }
    }
  }
}
function V() {
  let t = "X-SignalR-User-Agent";
  return b.isNode && (t = "User-Agent"), [t, _t(dt, mt(), Ct(), vt())];
}
function _t(t, e, o, r) {
  let s = "Microsoft SignalR/";
  const i = t.split(".");
  return s += `${i[0]}.${i[1]}`, s += ` (${t}; `, e && e !== "" ? s += `${e}; ` : s += "Unknown OS; ", s += `${o}`, r ? s += `; ${r}` : s += "; Unknown Runtime Version", s += ")", s;
}
function mt() {
  if (b.isNode)
    switch (process.platform) {
      case "win32":
        return "Windows NT";
      case "darwin":
        return "macOS";
      case "linux":
        return "Linux";
      default:
        return process.platform;
    }
  else
    return "";
}
function vt() {
  if (b.isNode)
    return process.versions.node;
}
function Ct() {
  return b.isNode ? "NodeJS" : "Browser";
}
function Q(t) {
  return t.stack ? t.stack : t.message ? t.message : `${t}`;
}
function yt() {
  if (typeof globalThis < "u")
    return globalThis;
  if (typeof self < "u")
    return self;
  if (typeof window < "u")
    return window;
  if (typeof global < "u")
    return global;
  throw new Error("could not find global");
}
class bt extends K {
  constructor(e) {
    if (super(), this._logger = e, typeof fetch > "u" || b.isNode) {
      const o = typeof __webpack_require__ == "function" ? __non_webpack_require__ : require;
      this._jar = new (o("tough-cookie")).CookieJar(), typeof fetch > "u" ? this._fetchType = o("node-fetch") : this._fetchType = fetch, this._fetchType = o("fetch-cookie")(this._fetchType, this._jar);
    } else
      this._fetchType = fetch.bind(yt());
    if (typeof AbortController > "u") {
      const o = typeof __webpack_require__ == "function" ? __non_webpack_require__ : require;
      this._abortControllerType = o("abort-controller");
    } else
      this._abortControllerType = AbortController;
  }
  /** @inheritDoc */
  async send(e) {
    if (e.abortSignal && e.abortSignal.aborted)
      throw new T();
    if (!e.method)
      throw new Error("No method defined.");
    if (!e.url)
      throw new Error("No url defined.");
    const o = new this._abortControllerType();
    let r;
    e.abortSignal && (e.abortSignal.onabort = () => {
      o.abort(), r = new T();
    });
    let s = null;
    if (e.timeout) {
      const u = e.timeout;
      s = setTimeout(() => {
        o.abort(), this._logger.log(a.Warning, "Timeout from HTTP request."), r = new ee();
      }, u);
    }
    e.content === "" && (e.content = void 0), e.content && (e.headers = e.headers || {}, R(e.content) ? e.headers["Content-Type"] = "application/octet-stream" : e.headers["Content-Type"] = "text/plain;charset=UTF-8");
    let i;
    try {
      i = await this._fetchType(e.url, {
        body: e.content,
        cache: "no-cache",
        credentials: e.withCredentials === !0 ? "include" : "same-origin",
        headers: {
          "X-Requested-With": "XMLHttpRequest",
          ...e.headers
        },
        method: e.method,
        mode: "cors",
        redirect: "follow",
        signal: o.signal
      });
    } catch (u) {
      throw r || (this._logger.log(a.Warning, `Error from HTTP request. ${u}.`), u);
    } finally {
      s && clearTimeout(s), e.abortSignal && (e.abortSignal.onabort = null);
    }
    if (!i.ok) {
      const u = await Se(i, "text");
      throw new A(u || i.statusText, i.status);
    }
    const c = await Se(i, e.responseType);
    return new Te(i.status, i.statusText, c);
  }
  getCookieString(e) {
    let o = "";
    return b.isNode && this._jar && this._jar.getCookies(e, (r, s) => o = s.join("; ")), o;
  }
}
function Se(t, e) {
  let o;
  switch (e) {
    case "arraybuffer":
      o = t.arrayBuffer();
      break;
    case "text":
      o = t.text();
      break;
    case "blob":
    case "document":
    case "json":
      throw new Error(`${e} is not supported.`);
    default:
      o = t.text();
      break;
  }
  return o;
}
class wt extends K {
  constructor(e) {
    super(), this._logger = e;
  }
  /** @inheritDoc */
  send(e) {
    return e.abortSignal && e.abortSignal.aborted ? Promise.reject(new T()) : e.method ? e.url ? new Promise((o, r) => {
      const s = new XMLHttpRequest();
      s.open(e.method, e.url, !0), s.withCredentials = e.withCredentials === void 0 ? !0 : e.withCredentials, s.setRequestHeader("X-Requested-With", "XMLHttpRequest"), e.content === "" && (e.content = void 0), e.content && (R(e.content) ? s.setRequestHeader("Content-Type", "application/octet-stream") : s.setRequestHeader("Content-Type", "text/plain;charset=UTF-8"));
      const i = e.headers;
      i && Object.keys(i).forEach((l) => {
        s.setRequestHeader(l, i[l]);
      }), e.responseType && (s.responseType = e.responseType), e.abortSignal && (e.abortSignal.onabort = () => {
        s.abort(), r(new T());
      }), e.timeout && (s.timeout = e.timeout), s.onload = () => {
        e.abortSignal && (e.abortSignal.onabort = null), s.status >= 200 && s.status < 300 ? o(new Te(s.status, s.statusText, s.response || s.responseText)) : r(new A(s.response || s.responseText || s.statusText, s.status));
      }, s.onerror = () => {
        this._logger.log(a.Warning, `Error from HTTP request. ${s.status}: ${s.statusText}.`), r(new A(s.statusText, s.status));
      }, s.ontimeout = () => {
        this._logger.log(a.Warning, "Timeout from HTTP request."), r(new ee());
      }, s.send(e.content);
    }) : Promise.reject(new Error("No url defined.")) : Promise.reject(new Error("No method defined."));
  }
}
class kt extends K {
  /** Creates a new instance of the {@link @microsoft/signalr.DefaultHttpClient}, using the provided {@link @microsoft/signalr.ILogger} to log messages. */
  constructor(e) {
    if (super(), typeof fetch < "u" || b.isNode)
      this._httpClient = new bt(e);
    else if (typeof XMLHttpRequest < "u")
      this._httpClient = new wt(e);
    else
      throw new Error("No usable HttpClient found.");
  }
  /** @inheritDoc */
  send(e) {
    return e.abortSignal && e.abortSignal.aborted ? Promise.reject(new T()) : e.method ? e.url ? this._httpClient.send(e) : Promise.reject(new Error("No url defined.")) : Promise.reject(new Error("No method defined."));
  }
  getCookieString(e) {
    return this._httpClient.getCookieString(e);
  }
}
class x {
  static write(e) {
    return `${e}${x.RecordSeparator}`;
  }
  static parse(e) {
    if (e[e.length - 1] !== x.RecordSeparator)
      throw new Error("Message is incomplete.");
    const o = e.split(x.RecordSeparator);
    return o.pop(), o;
  }
}
x.RecordSeparatorCode = 30;
x.RecordSeparator = String.fromCharCode(x.RecordSeparatorCode);
class St {
  // Handshake request is always JSON
  writeHandshakeRequest(e) {
    return x.write(JSON.stringify(e));
  }
  parseHandshakeResponse(e) {
    let o, r;
    if (R(e)) {
      const c = new Uint8Array(e), u = c.indexOf(x.RecordSeparatorCode);
      if (u === -1)
        throw new Error("Message is incomplete.");
      const d = u + 1;
      o = String.fromCharCode.apply(null, Array.prototype.slice.call(c.slice(0, d))), r = c.byteLength > d ? c.slice(d).buffer : null;
    } else {
      const c = e, u = c.indexOf(x.RecordSeparator);
      if (u === -1)
        throw new Error("Message is incomplete.");
      const d = u + 1;
      o = c.substring(0, d), r = c.length > d ? c.substring(d) : null;
    }
    const s = x.parse(o), i = JSON.parse(s[0]);
    if (i.type)
      throw new Error("Expected a handshake response from the server.");
    return [r, i];
  }
}
var g;
(function(t) {
  t[t.Invocation = 1] = "Invocation", t[t.StreamItem = 2] = "StreamItem", t[t.Completion = 3] = "Completion", t[t.StreamInvocation = 4] = "StreamInvocation", t[t.CancelInvocation = 5] = "CancelInvocation", t[t.Ping = 6] = "Ping", t[t.Close = 7] = "Close", t[t.Ack = 8] = "Ack", t[t.Sequence = 9] = "Sequence";
})(g || (g = {}));
class Et {
  constructor() {
    this.observers = [];
  }
  next(e) {
    for (const o of this.observers)
      o.next(e);
  }
  error(e) {
    for (const o of this.observers)
      o.error && o.error(e);
  }
  complete() {
    for (const e of this.observers)
      e.complete && e.complete();
  }
  subscribe(e) {
    return this.observers.push(e), new pt(this, e);
  }
}
class Pt {
  constructor(e, o, r) {
    this._bufferSize = 1e5, this._messages = [], this._totalMessageCount = 0, this._waitForSequenceMessage = !1, this._nextReceivingSequenceId = 1, this._latestReceivedSequenceId = 0, this._bufferedByteCount = 0, this._reconnectInProgress = !1, this._protocol = e, this._connection = o, this._bufferSize = r;
  }
  async _send(e) {
    const o = this._protocol.writeMessage(e);
    let r = Promise.resolve();
    if (this._isInvocationMessage(e)) {
      this._totalMessageCount++;
      let s = () => {
      }, i = () => {
      };
      R(o) ? this._bufferedByteCount += o.byteLength : this._bufferedByteCount += o.length, this._bufferedByteCount >= this._bufferSize && (r = new Promise((l, c) => {
        s = l, i = c;
      })), this._messages.push(new xt(o, this._totalMessageCount, s, i));
    }
    try {
      this._reconnectInProgress || await this._connection.send(o);
    } catch {
      this._disconnected();
    }
    await r;
  }
  _ack(e) {
    let o = -1;
    for (let r = 0; r < this._messages.length; r++) {
      const s = this._messages[r];
      if (s._id <= e.sequenceId)
        o = r, R(s._message) ? this._bufferedByteCount -= s._message.byteLength : this._bufferedByteCount -= s._message.length, s._resolver();
      else if (this._bufferedByteCount < this._bufferSize)
        s._resolver();
      else
        break;
    }
    o !== -1 && (this._messages = this._messages.slice(o + 1));
  }
  _shouldProcessMessage(e) {
    if (this._waitForSequenceMessage)
      return e.type !== g.Sequence ? !1 : (this._waitForSequenceMessage = !1, !0);
    if (!this._isInvocationMessage(e))
      return !0;
    const o = this._nextReceivingSequenceId;
    return this._nextReceivingSequenceId++, o <= this._latestReceivedSequenceId ? (o === this._latestReceivedSequenceId && this._ackTimer(), !1) : (this._latestReceivedSequenceId = o, this._ackTimer(), !0);
  }
  _resetSequence(e) {
    if (e.sequenceId > this._nextReceivingSequenceId) {
      this._connection.stop(new Error("Sequence ID greater than amount of messages we've received."));
      return;
    }
    this._nextReceivingSequenceId = e.sequenceId;
  }
  _disconnected() {
    this._reconnectInProgress = !0, this._waitForSequenceMessage = !0;
  }
  async _resend() {
    const e = this._messages.length !== 0 ? this._messages[0]._id : this._totalMessageCount + 1;
    await this._connection.send(this._protocol.writeMessage({ type: g.Sequence, sequenceId: e }));
    const o = this._messages;
    for (const r of o)
      await this._connection.send(r._message);
    this._reconnectInProgress = !1;
  }
  _dispose(e) {
    e ?? (e = new Error("Unable to reconnect to server."));
    for (const o of this._messages)
      o._rejector(e);
  }
  _isInvocationMessage(e) {
    switch (e.type) {
      case g.Invocation:
      case g.StreamItem:
      case g.Completion:
      case g.StreamInvocation:
      case g.CancelInvocation:
        return !0;
      case g.Close:
      case g.Sequence:
      case g.Ping:
      case g.Ack:
        return !1;
    }
  }
  _ackTimer() {
    this._ackTimerHandle === void 0 && (this._ackTimerHandle = setTimeout(async () => {
      try {
        this._reconnectInProgress || await this._connection.send(this._protocol.writeMessage({ type: g.Ack, sequenceId: this._latestReceivedSequenceId }));
      } catch {
      }
      clearTimeout(this._ackTimerHandle), this._ackTimerHandle = void 0;
    }, 1e3));
  }
}
class xt {
  constructor(e, o, r, s) {
    this._message = e, this._id = o, this._resolver = r, this._rejector = s;
  }
}
const It = 30 * 1e3, Tt = 15 * 1e3, $t = 1e5;
var y;
(function(t) {
  t.Disconnected = "Disconnected", t.Connecting = "Connecting", t.Connected = "Connected", t.Disconnecting = "Disconnecting", t.Reconnecting = "Reconnecting";
})(y || (y = {}));
class te {
  /** @internal */
  // Using a public static factory method means we can have a private constructor and an _internal_
  // create method that can be used by HubConnectionBuilder. An "internal" constructor would just
  // be stripped away and the '.d.ts' file would have no constructor, which is interpreted as a
  // public parameter-less constructor.
  static create(e, o, r, s, i, l, c) {
    return new te(e, o, r, s, i, l, c);
  }
  constructor(e, o, r, s, i, l, c) {
    this._nextKeepAlive = 0, this._freezeEventListener = () => {
      this._logger.log(a.Warning, "The page is being frozen, this will likely lead to the connection being closed and messages being lost. For more information see the docs at https://learn.microsoft.com/aspnet/core/signalr/javascript-client#bsleep");
    }, k.isRequired(e, "connection"), k.isRequired(o, "logger"), k.isRequired(r, "protocol"), this.serverTimeoutInMilliseconds = i ?? It, this.keepAliveIntervalInMilliseconds = l ?? Tt, this._statefulReconnectBufferSize = c ?? $t, this._logger = o, this._protocol = r, this.connection = e, this._reconnectPolicy = s, this._handshakeProtocol = new St(), this.connection.onreceive = (u) => this._processIncomingData(u), this.connection.onclose = (u) => this._connectionClosed(u), this._callbacks = {}, this._methods = {}, this._closedCallbacks = [], this._reconnectingCallbacks = [], this._reconnectedCallbacks = [], this._invocationId = 0, this._receivedHandshakeResponse = !1, this._connectionState = y.Disconnected, this._connectionStarted = !1, this._cachedPingMessage = this._protocol.writeMessage({ type: g.Ping });
  }
  /** Indicates the state of the {@link HubConnection} to the server. */
  get state() {
    return this._connectionState;
  }
  /** Represents the connection id of the {@link HubConnection} on the server. The connection id will be null when the connection is either
   *  in the disconnected state or if the negotiation step was skipped.
   */
  get connectionId() {
    return this.connection && this.connection.connectionId || null;
  }
  /** Indicates the url of the {@link HubConnection} to the server. */
  get baseUrl() {
    return this.connection.baseUrl || "";
  }
  /**
   * Sets a new url for the HubConnection. Note that the url can only be changed when the connection is in either the Disconnected or
   * Reconnecting states.
   * @param {string} url The url to connect to.
   */
  set baseUrl(e) {
    if (this._connectionState !== y.Disconnected && this._connectionState !== y.Reconnecting)
      throw new Error("The HubConnection must be in the Disconnected or Reconnecting state to change the url.");
    if (!e)
      throw new Error("The HubConnection url must be a valid url.");
    this.connection.baseUrl = e;
  }
  /** Starts the connection.
   *
   * @returns {Promise<void>} A Promise that resolves when the connection has been successfully established, or rejects with an error.
   */
  start() {
    return this._startPromise = this._startWithStateTransitions(), this._startPromise;
  }
  async _startWithStateTransitions() {
    if (this._connectionState !== y.Disconnected)
      return Promise.reject(new Error("Cannot start a HubConnection that is not in the 'Disconnected' state."));
    this._connectionState = y.Connecting, this._logger.log(a.Debug, "Starting HubConnection.");
    try {
      await this._startInternal(), b.isBrowser && window.document.addEventListener("freeze", this._freezeEventListener), this._connectionState = y.Connected, this._connectionStarted = !0, this._logger.log(a.Debug, "HubConnection connected successfully.");
    } catch (e) {
      return this._connectionState = y.Disconnected, this._logger.log(a.Debug, `HubConnection failed to start successfully because of error '${e}'.`), Promise.reject(e);
    }
  }
  async _startInternal() {
    this._stopDuringStartError = void 0, this._receivedHandshakeResponse = !1;
    const e = new Promise((o, r) => {
      this._handshakeResolver = o, this._handshakeRejecter = r;
    });
    await this.connection.start(this._protocol.transferFormat);
    try {
      let o = this._protocol.version;
      this.connection.features.reconnect || (o = 1);
      const r = {
        protocol: this._protocol.name,
        version: o
      };
      if (this._logger.log(a.Debug, "Sending handshake request."), await this._sendMessage(this._handshakeProtocol.writeHandshakeRequest(r)), this._logger.log(a.Information, `Using HubProtocol '${this._protocol.name}'.`), this._cleanupTimeout(), this._resetTimeoutPeriod(), this._resetKeepAliveInterval(), await e, this._stopDuringStartError)
        throw this._stopDuringStartError;
      this.connection.features.reconnect && (this._messageBuffer = new Pt(this._protocol, this.connection, this._statefulReconnectBufferSize), this.connection.features.disconnected = this._messageBuffer._disconnected.bind(this._messageBuffer), this.connection.features.resend = () => {
        if (this._messageBuffer)
          return this._messageBuffer._resend();
      }), this.connection.features.inherentKeepAlive || await this._sendMessage(this._cachedPingMessage);
    } catch (o) {
      throw this._logger.log(a.Debug, `Hub handshake failed with error '${o}' during start(). Stopping HubConnection.`), this._cleanupTimeout(), this._cleanupPingTimer(), await this.connection.stop(o), o;
    }
  }
  /** Stops the connection.
   *
   * @returns {Promise<void>} A Promise that resolves when the connection has been successfully terminated, or rejects with an error.
   */
  async stop() {
    const e = this._startPromise;
    this.connection.features.reconnect = !1, this._stopPromise = this._stopInternal(), await this._stopPromise;
    try {
      await e;
    } catch {
    }
  }
  _stopInternal(e) {
    if (this._connectionState === y.Disconnected)
      return this._logger.log(a.Debug, `Call to HubConnection.stop(${e}) ignored because it is already in the disconnected state.`), Promise.resolve();
    if (this._connectionState === y.Disconnecting)
      return this._logger.log(a.Debug, `Call to HttpConnection.stop(${e}) ignored because the connection is already in the disconnecting state.`), this._stopPromise;
    const o = this._connectionState;
    return this._connectionState = y.Disconnecting, this._logger.log(a.Debug, "Stopping HubConnection."), this._reconnectDelayHandle ? (this._logger.log(a.Debug, "Connection stopped during reconnect delay. Done reconnecting."), clearTimeout(this._reconnectDelayHandle), this._reconnectDelayHandle = void 0, this._completeClose(), Promise.resolve()) : (o === y.Connected && this._sendCloseMessage(), this._cleanupTimeout(), this._cleanupPingTimer(), this._stopDuringStartError = e || new T("The connection was stopped before the hub handshake could complete."), this.connection.stop(e));
  }
  async _sendCloseMessage() {
    try {
      await this._sendWithProtocol(this._createCloseMessage());
    } catch {
    }
  }
  /** Invokes a streaming hub method on the server using the specified name and arguments.
   *
   * @typeparam T The type of the items returned by the server.
   * @param {string} methodName The name of the server method to invoke.
   * @param {any[]} args The arguments used to invoke the server method.
   * @returns {IStreamResult<T>} An object that yields results from the server as they are received.
   */
  stream(e, ...o) {
    const [r, s] = this._replaceStreamingParams(o), i = this._createStreamInvocation(e, o, s);
    let l;
    const c = new Et();
    return c.cancelCallback = () => {
      const u = this._createCancelInvocation(i.invocationId);
      return delete this._callbacks[i.invocationId], l.then(() => this._sendWithProtocol(u));
    }, this._callbacks[i.invocationId] = (u, d) => {
      if (d) {
        c.error(d);
        return;
      } else u && (u.type === g.Completion ? u.error ? c.error(new Error(u.error)) : c.complete() : c.next(u.item));
    }, l = this._sendWithProtocol(i).catch((u) => {
      c.error(u), delete this._callbacks[i.invocationId];
    }), this._launchStreams(r, l), c;
  }
  _sendMessage(e) {
    return this._resetKeepAliveInterval(), this.connection.send(e);
  }
  /**
   * Sends a js object to the server.
   * @param message The js object to serialize and send.
   */
  _sendWithProtocol(e) {
    return this._messageBuffer ? this._messageBuffer._send(e) : this._sendMessage(this._protocol.writeMessage(e));
  }
  /** Invokes a hub method on the server using the specified name and arguments. Does not wait for a response from the receiver.
   *
   * The Promise returned by this method resolves when the client has sent the invocation to the server. The server may still
   * be processing the invocation.
   *
   * @param {string} methodName The name of the server method to invoke.
   * @param {any[]} args The arguments used to invoke the server method.
   * @returns {Promise<void>} A Promise that resolves when the invocation has been successfully sent, or rejects with an error.
   */
  send(e, ...o) {
    const [r, s] = this._replaceStreamingParams(o), i = this._sendWithProtocol(this._createInvocation(e, o, !0, s));
    return this._launchStreams(r, i), i;
  }
  /** Invokes a hub method on the server using the specified name and arguments.
   *
   * The Promise returned by this method resolves when the server indicates it has finished invoking the method. When the promise
   * resolves, the server has finished invoking the method. If the server method returns a result, it is produced as the result of
   * resolving the Promise.
   *
   * @typeparam T The expected return type.
   * @param {string} methodName The name of the server method to invoke.
   * @param {any[]} args The arguments used to invoke the server method.
   * @returns {Promise<T>} A Promise that resolves with the result of the server method (if any), or rejects with an error.
   */
  invoke(e, ...o) {
    const [r, s] = this._replaceStreamingParams(o), i = this._createInvocation(e, o, !1, s);
    return new Promise((c, u) => {
      this._callbacks[i.invocationId] = (f, p) => {
        if (p) {
          u(p);
          return;
        } else f && (f.type === g.Completion ? f.error ? u(new Error(f.error)) : c(f.result) : u(new Error(`Unexpected message type: ${f.type}`)));
      };
      const d = this._sendWithProtocol(i).catch((f) => {
        u(f), delete this._callbacks[i.invocationId];
      });
      this._launchStreams(r, d);
    });
  }
  on(e, o) {
    !e || !o || (e = e.toLowerCase(), this._methods[e] || (this._methods[e] = []), this._methods[e].indexOf(o) === -1 && this._methods[e].push(o));
  }
  off(e, o) {
    if (!e)
      return;
    e = e.toLowerCase();
    const r = this._methods[e];
    if (r)
      if (o) {
        const s = r.indexOf(o);
        s !== -1 && (r.splice(s, 1), r.length === 0 && delete this._methods[e]);
      } else
        delete this._methods[e];
  }
  /** Registers a handler that will be invoked when the connection is closed.
   *
   * @param {Function} callback The handler that will be invoked when the connection is closed. Optionally receives a single argument containing the error that caused the connection to close (if any).
   */
  onclose(e) {
    e && this._closedCallbacks.push(e);
  }
  /** Registers a handler that will be invoked when the connection starts reconnecting.
   *
   * @param {Function} callback The handler that will be invoked when the connection starts reconnecting. Optionally receives a single argument containing the error that caused the connection to start reconnecting (if any).
   */
  onreconnecting(e) {
    e && this._reconnectingCallbacks.push(e);
  }
  /** Registers a handler that will be invoked when the connection successfully reconnects.
   *
   * @param {Function} callback The handler that will be invoked when the connection successfully reconnects.
   */
  onreconnected(e) {
    e && this._reconnectedCallbacks.push(e);
  }
  _processIncomingData(e) {
    if (this._cleanupTimeout(), this._receivedHandshakeResponse || (e = this._processHandshakeResponse(e), this._receivedHandshakeResponse = !0), e) {
      const o = this._protocol.parseMessages(e, this._logger);
      for (const r of o)
        if (!(this._messageBuffer && !this._messageBuffer._shouldProcessMessage(r)))
          switch (r.type) {
            case g.Invocation:
              this._invokeClientMethod(r).catch((s) => {
                this._logger.log(a.Error, `Invoke client method threw error: ${Q(s)}`);
              });
              break;
            case g.StreamItem:
            case g.Completion: {
              const s = this._callbacks[r.invocationId];
              if (s) {
                r.type === g.Completion && delete this._callbacks[r.invocationId];
                try {
                  s(r);
                } catch (i) {
                  this._logger.log(a.Error, `Stream callback threw error: ${Q(i)}`);
                }
              }
              break;
            }
            case g.Ping:
              break;
            case g.Close: {
              this._logger.log(a.Information, "Close message received from server.");
              const s = r.error ? new Error("Server returned an error on close: " + r.error) : void 0;
              r.allowReconnect === !0 ? this.connection.stop(s) : this._stopPromise = this._stopInternal(s);
              break;
            }
            case g.Ack:
              this._messageBuffer && this._messageBuffer._ack(r);
              break;
            case g.Sequence:
              this._messageBuffer && this._messageBuffer._resetSequence(r);
              break;
            default:
              this._logger.log(a.Warning, `Invalid message type: ${r.type}.`);
              break;
          }
    }
    this._resetTimeoutPeriod();
  }
  _processHandshakeResponse(e) {
    let o, r;
    try {
      [r, o] = this._handshakeProtocol.parseHandshakeResponse(e);
    } catch (s) {
      const i = "Error parsing handshake response: " + s;
      this._logger.log(a.Error, i);
      const l = new Error(i);
      throw this._handshakeRejecter(l), l;
    }
    if (o.error) {
      const s = "Server returned handshake error: " + o.error;
      this._logger.log(a.Error, s);
      const i = new Error(s);
      throw this._handshakeRejecter(i), i;
    } else
      this._logger.log(a.Debug, "Server handshake complete.");
    return this._handshakeResolver(), r;
  }
  _resetKeepAliveInterval() {
    this.connection.features.inherentKeepAlive || (this._nextKeepAlive = (/* @__PURE__ */ new Date()).getTime() + this.keepAliveIntervalInMilliseconds, this._cleanupPingTimer());
  }
  _resetTimeoutPeriod() {
    if (!this.connection.features || !this.connection.features.inherentKeepAlive) {
      this._timeoutHandle = setTimeout(() => this.serverTimeout(), this.serverTimeoutInMilliseconds);
      let e = this._nextKeepAlive - (/* @__PURE__ */ new Date()).getTime();
      if (e < 0) {
        this._connectionState === y.Connected && this._trySendPingMessage();
        return;
      }
      this._pingServerHandle === void 0 && (e < 0 && (e = 0), this._pingServerHandle = setTimeout(async () => {
        this._connectionState === y.Connected && await this._trySendPingMessage();
      }, e));
    }
  }
  // eslint-disable-next-line @typescript-eslint/naming-convention
  serverTimeout() {
    this.connection.stop(new Error("Server timeout elapsed without receiving a message from the server."));
  }
  async _invokeClientMethod(e) {
    const o = e.target.toLowerCase(), r = this._methods[o];
    if (!r) {
      this._logger.log(a.Warning, `No client method with the name '${o}' found.`), e.invocationId && (this._logger.log(a.Warning, `No result given for '${o}' method and invocation ID '${e.invocationId}'.`), await this._sendWithProtocol(this._createCompletionMessage(e.invocationId, "Client didn't provide a result.", null)));
      return;
    }
    const s = r.slice(), i = !!e.invocationId;
    let l, c, u;
    for (const d of s)
      try {
        const f = l;
        l = await d.apply(this, e.arguments), i && l && f && (this._logger.log(a.Error, `Multiple results provided for '${o}'. Sending error to server.`), u = this._createCompletionMessage(e.invocationId, "Client provided multiple results.", null)), c = void 0;
      } catch (f) {
        c = f, this._logger.log(a.Error, `A callback for the method '${o}' threw error '${f}'.`);
      }
    u ? await this._sendWithProtocol(u) : i ? (c ? u = this._createCompletionMessage(e.invocationId, `${c}`, null) : l !== void 0 ? u = this._createCompletionMessage(e.invocationId, null, l) : (this._logger.log(a.Warning, `No result given for '${o}' method and invocation ID '${e.invocationId}'.`), u = this._createCompletionMessage(e.invocationId, "Client didn't provide a result.", null)), await this._sendWithProtocol(u)) : l && this._logger.log(a.Error, `Result given for '${o}' method but server is not expecting a result.`);
  }
  _connectionClosed(e) {
    this._logger.log(a.Debug, `HubConnection.connectionClosed(${e}) called while in state ${this._connectionState}.`), this._stopDuringStartError = this._stopDuringStartError || e || new T("The underlying connection was closed before the hub handshake could complete."), this._handshakeResolver && this._handshakeResolver(), this._cancelCallbacksWithError(e || new Error("Invocation canceled due to the underlying connection being closed.")), this._cleanupTimeout(), this._cleanupPingTimer(), this._connectionState === y.Disconnecting ? this._completeClose(e) : this._connectionState === y.Connected && this._reconnectPolicy ? this._reconnect(e) : this._connectionState === y.Connected && this._completeClose(e);
  }
  _completeClose(e) {
    if (this._connectionStarted) {
      this._connectionState = y.Disconnected, this._connectionStarted = !1, this._messageBuffer && (this._messageBuffer._dispose(e ?? new Error("Connection closed.")), this._messageBuffer = void 0), b.isBrowser && window.document.removeEventListener("freeze", this._freezeEventListener);
      try {
        this._closedCallbacks.forEach((o) => o.apply(this, [e]));
      } catch (o) {
        this._logger.log(a.Error, `An onclose callback called with error '${e}' threw error '${o}'.`);
      }
    }
  }
  async _reconnect(e) {
    const o = Date.now();
    let r = 0, s = e !== void 0 ? e : new Error("Attempting to reconnect due to a unknown error."), i = this._getNextRetryDelay(r, 0, s);
    if (i === null) {
      this._logger.log(a.Debug, "Connection not reconnecting because the IRetryPolicy returned null on the first reconnect attempt."), this._completeClose(e);
      return;
    }
    if (this._connectionState = y.Reconnecting, e ? this._logger.log(a.Information, `Connection reconnecting because of error '${e}'.`) : this._logger.log(a.Information, "Connection reconnecting."), this._reconnectingCallbacks.length !== 0) {
      try {
        this._reconnectingCallbacks.forEach((l) => l.apply(this, [e]));
      } catch (l) {
        this._logger.log(a.Error, `An onreconnecting callback called with error '${e}' threw error '${l}'.`);
      }
      if (this._connectionState !== y.Reconnecting) {
        this._logger.log(a.Debug, "Connection left the reconnecting state in onreconnecting callback. Done reconnecting.");
        return;
      }
    }
    for (; i !== null; ) {
      if (this._logger.log(a.Information, `Reconnect attempt number ${r + 1} will start in ${i} ms.`), await new Promise((l) => {
        this._reconnectDelayHandle = setTimeout(l, i);
      }), this._reconnectDelayHandle = void 0, this._connectionState !== y.Reconnecting) {
        this._logger.log(a.Debug, "Connection left the reconnecting state during reconnect delay. Done reconnecting.");
        return;
      }
      try {
        if (await this._startInternal(), this._connectionState = y.Connected, this._logger.log(a.Information, "HubConnection reconnected successfully."), this._reconnectedCallbacks.length !== 0)
          try {
            this._reconnectedCallbacks.forEach((l) => l.apply(this, [this.connection.connectionId]));
          } catch (l) {
            this._logger.log(a.Error, `An onreconnected callback called with connectionId '${this.connection.connectionId}; threw error '${l}'.`);
          }
        return;
      } catch (l) {
        if (this._logger.log(a.Information, `Reconnect attempt failed because of error '${l}'.`), this._connectionState !== y.Reconnecting) {
          this._logger.log(a.Debug, `Connection moved to the '${this._connectionState}' from the reconnecting state during reconnect attempt. Done reconnecting.`), this._connectionState === y.Disconnecting && this._completeClose();
          return;
        }
        r++, s = l instanceof Error ? l : new Error(l.toString()), i = this._getNextRetryDelay(r, Date.now() - o, s);
      }
    }
    this._logger.log(a.Information, `Reconnect retries have been exhausted after ${Date.now() - o} ms and ${r} failed attempts. Connection disconnecting.`), this._completeClose();
  }
  _getNextRetryDelay(e, o, r) {
    try {
      return this._reconnectPolicy.nextRetryDelayInMilliseconds({
        elapsedMilliseconds: o,
        previousRetryCount: e,
        retryReason: r
      });
    } catch (s) {
      return this._logger.log(a.Error, `IRetryPolicy.nextRetryDelayInMilliseconds(${e}, ${o}) threw error '${s}'.`), null;
    }
  }
  _cancelCallbacksWithError(e) {
    const o = this._callbacks;
    this._callbacks = {}, Object.keys(o).forEach((r) => {
      const s = o[r];
      try {
        s(null, e);
      } catch (i) {
        this._logger.log(a.Error, `Stream 'error' callback called with '${e}' threw error: ${Q(i)}`);
      }
    });
  }
  _cleanupPingTimer() {
    this._pingServerHandle && (clearTimeout(this._pingServerHandle), this._pingServerHandle = void 0);
  }
  _cleanupTimeout() {
    this._timeoutHandle && clearTimeout(this._timeoutHandle);
  }
  _createInvocation(e, o, r, s) {
    if (r)
      return s.length !== 0 ? {
        target: e,
        arguments: o,
        streamIds: s,
        type: g.Invocation
      } : {
        target: e,
        arguments: o,
        type: g.Invocation
      };
    {
      const i = this._invocationId;
      return this._invocationId++, s.length !== 0 ? {
        target: e,
        arguments: o,
        invocationId: i.toString(),
        streamIds: s,
        type: g.Invocation
      } : {
        target: e,
        arguments: o,
        invocationId: i.toString(),
        type: g.Invocation
      };
    }
  }
  _launchStreams(e, o) {
    if (e.length !== 0) {
      o || (o = Promise.resolve());
      for (const r in e)
        e[r].subscribe({
          complete: () => {
            o = o.then(() => this._sendWithProtocol(this._createCompletionMessage(r)));
          },
          error: (s) => {
            let i;
            s instanceof Error ? i = s.message : s && s.toString ? i = s.toString() : i = "Unknown error", o = o.then(() => this._sendWithProtocol(this._createCompletionMessage(r, i)));
          },
          next: (s) => {
            o = o.then(() => this._sendWithProtocol(this._createStreamItemMessage(r, s)));
          }
        });
    }
  }
  _replaceStreamingParams(e) {
    const o = [], r = [];
    for (let s = 0; s < e.length; s++) {
      const i = e[s];
      if (this._isObservable(i)) {
        const l = this._invocationId;
        this._invocationId++, o[l] = i, r.push(l.toString()), e.splice(s, 1);
      }
    }
    return [o, r];
  }
  _isObservable(e) {
    return e && e.subscribe && typeof e.subscribe == "function";
  }
  _createStreamInvocation(e, o, r) {
    const s = this._invocationId;
    return this._invocationId++, r.length !== 0 ? {
      target: e,
      arguments: o,
      invocationId: s.toString(),
      streamIds: r,
      type: g.StreamInvocation
    } : {
      target: e,
      arguments: o,
      invocationId: s.toString(),
      type: g.StreamInvocation
    };
  }
  _createCancelInvocation(e) {
    return {
      invocationId: e,
      type: g.CancelInvocation
    };
  }
  _createStreamItemMessage(e, o) {
    return {
      invocationId: e,
      item: o,
      type: g.StreamItem
    };
  }
  _createCompletionMessage(e, o, r) {
    return o ? {
      error: o,
      invocationId: e,
      type: g.Completion
    } : {
      invocationId: e,
      result: r,
      type: g.Completion
    };
  }
  _createCloseMessage() {
    return { type: g.Close };
  }
  async _trySendPingMessage() {
    try {
      await this._sendMessage(this._cachedPingMessage);
    } catch {
      this._cleanupPingTimer();
    }
  }
}
const Nt = [0, 2e3, 1e4, 3e4, null];
class Ee {
  constructor(e) {
    this._retryDelays = e !== void 0 ? [...e, null] : Nt;
  }
  nextRetryDelayInMilliseconds(e) {
    return this._retryDelays[e.previousRetryCount];
  }
}
class D {
}
D.Authorization = "Authorization";
D.Cookie = "Cookie";
class At extends K {
  constructor(e, o) {
    super(), this._innerClient = e, this._accessTokenFactory = o;
  }
  async send(e) {
    let o = !0;
    this._accessTokenFactory && (!this._accessToken || e.url && e.url.indexOf("/negotiate?") > 0) && (o = !1, this._accessToken = await this._accessTokenFactory()), this._setAuthorizationHeader(e);
    const r = await this._innerClient.send(e);
    return o && r.statusCode === 401 && this._accessTokenFactory ? (this._accessToken = await this._accessTokenFactory(), this._setAuthorizationHeader(e), await this._innerClient.send(e)) : r;
  }
  _setAuthorizationHeader(e) {
    e.headers || (e.headers = {}), this._accessToken ? e.headers[D.Authorization] = `Bearer ${this._accessToken}` : this._accessTokenFactory && e.headers[D.Authorization] && delete e.headers[D.Authorization];
  }
  getCookieString(e) {
    return this._innerClient.getCookieString(e);
  }
}
var S;
(function(t) {
  t[t.None = 0] = "None", t[t.WebSockets = 1] = "WebSockets", t[t.ServerSentEvents = 2] = "ServerSentEvents", t[t.LongPolling = 4] = "LongPolling";
})(S || (S = {}));
var E;
(function(t) {
  t[t.Text = 1] = "Text", t[t.Binary = 2] = "Binary";
})(E || (E = {}));
let Dt = class {
  constructor() {
    this._isAborted = !1, this.onabort = null;
  }
  abort() {
    this._isAborted || (this._isAborted = !0, this.onabort && this.onabort());
  }
  get signal() {
    return this;
  }
  get aborted() {
    return this._isAborted;
  }
};
class Pe {
  // This is an internal type, not exported from 'index' so this is really just internal.
  get pollAborted() {
    return this._pollAbort.aborted;
  }
  constructor(e, o, r) {
    this._httpClient = e, this._logger = o, this._pollAbort = new Dt(), this._options = r, this._running = !1, this.onreceive = null, this.onclose = null;
  }
  async connect(e, o) {
    if (k.isRequired(e, "url"), k.isRequired(o, "transferFormat"), k.isIn(o, E, "transferFormat"), this._url = e, this._logger.log(a.Trace, "(LongPolling transport) Connecting."), o === E.Binary && typeof XMLHttpRequest < "u" && typeof new XMLHttpRequest().responseType != "string")
      throw new Error("Binary protocols over XmlHttpRequest not implementing advanced features are not supported.");
    const [r, s] = V(), i = { [r]: s, ...this._options.headers }, l = {
      abortSignal: this._pollAbort.signal,
      headers: i,
      timeout: 1e5,
      withCredentials: this._options.withCredentials
    };
    o === E.Binary && (l.responseType = "arraybuffer");
    const c = `${e}&_=${Date.now()}`;
    this._logger.log(a.Trace, `(LongPolling transport) polling: ${c}.`);
    const u = await this._httpClient.get(c, l);
    u.statusCode !== 200 ? (this._logger.log(a.Error, `(LongPolling transport) Unexpected response code: ${u.statusCode}.`), this._closeError = new A(u.statusText || "", u.statusCode), this._running = !1) : this._running = !0, this._receiving = this._poll(this._url, l);
  }
  async _poll(e, o) {
    try {
      for (; this._running; )
        try {
          const r = `${e}&_=${Date.now()}`;
          this._logger.log(a.Trace, `(LongPolling transport) polling: ${r}.`);
          const s = await this._httpClient.get(r, o);
          s.statusCode === 204 ? (this._logger.log(a.Information, "(LongPolling transport) Poll terminated by server."), this._running = !1) : s.statusCode !== 200 ? (this._logger.log(a.Error, `(LongPolling transport) Unexpected response code: ${s.statusCode}.`), this._closeError = new A(s.statusText || "", s.statusCode), this._running = !1) : s.content ? (this._logger.log(a.Trace, `(LongPolling transport) data received. ${z(s.content, this._options.logMessageContent)}.`), this.onreceive && this.onreceive(s.content)) : this._logger.log(a.Trace, "(LongPolling transport) Poll timed out, reissuing.");
        } catch (r) {
          this._running ? r instanceof ee ? this._logger.log(a.Trace, "(LongPolling transport) Poll timed out, reissuing.") : (this._closeError = r, this._running = !1) : this._logger.log(a.Trace, `(LongPolling transport) Poll errored after shutdown: ${r.message}`);
        }
    } finally {
      this._logger.log(a.Trace, "(LongPolling transport) Polling complete."), this.pollAborted || this._raiseOnClose();
    }
  }
  async send(e) {
    return this._running ? $e(this._logger, "LongPolling", this._httpClient, this._url, e, this._options) : Promise.reject(new Error("Cannot send until the transport is connected"));
  }
  async stop() {
    this._logger.log(a.Trace, "(LongPolling transport) Stopping polling."), this._running = !1, this._pollAbort.abort();
    try {
      await this._receiving, this._logger.log(a.Trace, `(LongPolling transport) sending DELETE request to ${this._url}.`);
      const e = {}, [o, r] = V();
      e[o] = r;
      const s = {
        headers: { ...e, ...this._options.headers },
        timeout: this._options.timeout,
        withCredentials: this._options.withCredentials
      };
      let i;
      try {
        await this._httpClient.delete(this._url, s);
      } catch (l) {
        i = l;
      }
      i ? i instanceof A && (i.statusCode === 404 ? this._logger.log(a.Trace, "(LongPolling transport) A 404 response was returned from sending a DELETE request.") : this._logger.log(a.Trace, `(LongPolling transport) Error sending a DELETE request: ${i}`)) : this._logger.log(a.Trace, "(LongPolling transport) DELETE request accepted.");
    } finally {
      this._logger.log(a.Trace, "(LongPolling transport) Stop finished."), this._raiseOnClose();
    }
  }
  _raiseOnClose() {
    if (this.onclose) {
      let e = "(LongPolling transport) Firing onclose event.";
      this._closeError && (e += " Error: " + this._closeError), this._logger.log(a.Trace, e), this.onclose(this._closeError);
    }
  }
}
class Rt {
  constructor(e, o, r, s) {
    this._httpClient = e, this._accessToken = o, this._logger = r, this._options = s, this.onreceive = null, this.onclose = null;
  }
  async connect(e, o) {
    return k.isRequired(e, "url"), k.isRequired(o, "transferFormat"), k.isIn(o, E, "transferFormat"), this._logger.log(a.Trace, "(SSE transport) Connecting."), this._url = e, this._accessToken && (e += (e.indexOf("?") < 0 ? "?" : "&") + `access_token=${encodeURIComponent(this._accessToken)}`), new Promise((r, s) => {
      let i = !1;
      if (o !== E.Text) {
        s(new Error("The Server-Sent Events transport only supports the 'Text' transfer format"));
        return;
      }
      let l;
      if (b.isBrowser || b.isWebWorker)
        l = new this._options.EventSource(e, { withCredentials: this._options.withCredentials });
      else {
        const c = this._httpClient.getCookieString(e), u = {};
        u.Cookie = c;
        const [d, f] = V();
        u[d] = f, l = new this._options.EventSource(e, { withCredentials: this._options.withCredentials, headers: { ...u, ...this._options.headers } });
      }
      try {
        l.onmessage = (c) => {
          if (this.onreceive)
            try {
              this._logger.log(a.Trace, `(SSE transport) data received. ${z(c.data, this._options.logMessageContent)}.`), this.onreceive(c.data);
            } catch (u) {
              this._close(u);
              return;
            }
        }, l.onerror = (c) => {
          i ? this._close() : s(new Error("EventSource failed to connect. The connection could not be found on the server, either the connection ID is not present on the server, or a proxy is refusing/buffering the connection. If you have multiple servers check that sticky sessions are enabled."));
        }, l.onopen = () => {
          this._logger.log(a.Information, `SSE connected to ${this._url}`), this._eventSource = l, i = !0, r();
        };
      } catch (c) {
        s(c);
        return;
      }
    });
  }
  async send(e) {
    return this._eventSource ? $e(this._logger, "SSE", this._httpClient, this._url, e, this._options) : Promise.reject(new Error("Cannot send until the transport is connected"));
  }
  stop() {
    return this._close(), Promise.resolve();
  }
  _close(e) {
    this._eventSource && (this._eventSource.close(), this._eventSource = void 0, this.onclose && this.onclose(e));
  }
}
class Ot {
  constructor(e, o, r, s, i, l) {
    this._logger = r, this._accessTokenFactory = o, this._logMessageContent = s, this._webSocketConstructor = i, this._httpClient = e, this.onreceive = null, this.onclose = null, this._headers = l;
  }
  async connect(e, o) {
    k.isRequired(e, "url"), k.isRequired(o, "transferFormat"), k.isIn(o, E, "transferFormat"), this._logger.log(a.Trace, "(WebSockets transport) Connecting.");
    let r;
    return this._accessTokenFactory && (r = await this._accessTokenFactory()), new Promise((s, i) => {
      e = e.replace(/^http/, "ws");
      let l;
      const c = this._httpClient.getCookieString(e);
      let u = !1;
      if (b.isNode || b.isReactNative) {
        const d = {}, [f, p] = V();
        d[f] = p, r && (d[D.Authorization] = `Bearer ${r}`), c && (d[D.Cookie] = c), l = new this._webSocketConstructor(e, void 0, {
          headers: { ...d, ...this._headers }
        });
      } else
        r && (e += (e.indexOf("?") < 0 ? "?" : "&") + `access_token=${encodeURIComponent(r)}`);
      l || (l = new this._webSocketConstructor(e)), o === E.Binary && (l.binaryType = "arraybuffer"), l.onopen = (d) => {
        this._logger.log(a.Information, `WebSocket connected to ${e}.`), this._webSocket = l, u = !0, s();
      }, l.onerror = (d) => {
        let f = null;
        typeof ErrorEvent < "u" && d instanceof ErrorEvent ? f = d.error : f = "There was an error with the transport", this._logger.log(a.Information, `(WebSockets transport) ${f}.`);
      }, l.onmessage = (d) => {
        if (this._logger.log(a.Trace, `(WebSockets transport) data received. ${z(d.data, this._logMessageContent)}.`), this.onreceive)
          try {
            this.onreceive(d.data);
          } catch (f) {
            this._close(f);
            return;
          }
      }, l.onclose = (d) => {
        if (u)
          this._close(d);
        else {
          let f = null;
          typeof ErrorEvent < "u" && d instanceof ErrorEvent ? f = d.error : f = "WebSocket failed to connect. The connection could not be found on the server, either the endpoint may not be a SignalR endpoint, the connection ID is not present on the server, or there is a proxy blocking WebSockets. If you have multiple servers check that sticky sessions are enabled.", i(new Error(f));
        }
      };
    });
  }
  send(e) {
    return this._webSocket && this._webSocket.readyState === this._webSocketConstructor.OPEN ? (this._logger.log(a.Trace, `(WebSockets transport) sending data. ${z(e, this._logMessageContent)}.`), this._webSocket.send(e), Promise.resolve()) : Promise.reject("WebSocket is not in the OPEN state");
  }
  stop() {
    return this._webSocket && this._close(void 0), Promise.resolve();
  }
  _close(e) {
    this._webSocket && (this._webSocket.onclose = () => {
    }, this._webSocket.onmessage = () => {
    }, this._webSocket.onerror = () => {
    }, this._webSocket.close(), this._webSocket = void 0), this._logger.log(a.Trace, "(WebSockets transport) socket closed."), this.onclose && (this._isCloseEvent(e) && (e.wasClean === !1 || e.code !== 1e3) ? this.onclose(new Error(`WebSocket closed with status code: ${e.code} (${e.reason || "no reason given"}).`)) : e instanceof Error ? this.onclose(e) : this.onclose());
  }
  _isCloseEvent(e) {
    return e && typeof e.wasClean == "boolean" && typeof e.code == "number";
  }
}
const xe = 100;
class jt {
  constructor(e, o = {}) {
    if (this._stopPromiseResolver = () => {
    }, this.features = {}, this._negotiateVersion = 1, k.isRequired(e, "url"), this._logger = gt(o.logger), this.baseUrl = this._resolveUrl(e), o = o || {}, o.logMessageContent = o.logMessageContent === void 0 ? !1 : o.logMessageContent, typeof o.withCredentials == "boolean" || o.withCredentials === void 0)
      o.withCredentials = o.withCredentials === void 0 ? !0 : o.withCredentials;
    else
      throw new Error("withCredentials option was not a 'boolean' or 'undefined' value");
    o.timeout = o.timeout === void 0 ? 100 * 1e3 : o.timeout;
    let r = null, s = null;
    if (b.isNode && typeof require < "u") {
      const i = typeof __webpack_require__ == "function" ? __non_webpack_require__ : require;
      r = i("ws"), s = i("eventsource");
    }
    !b.isNode && typeof WebSocket < "u" && !o.WebSocket ? o.WebSocket = WebSocket : b.isNode && !o.WebSocket && r && (o.WebSocket = r), !b.isNode && typeof EventSource < "u" && !o.EventSource ? o.EventSource = EventSource : b.isNode && !o.EventSource && typeof s < "u" && (o.EventSource = s), this._httpClient = new At(o.httpClient || new kt(this._logger), o.accessTokenFactory), this._connectionState = "Disconnected", this._connectionStarted = !1, this._options = o, this.onreceive = null, this.onclose = null;
  }
  async start(e) {
    if (e = e || E.Binary, k.isIn(e, E, "transferFormat"), this._logger.log(a.Debug, `Starting connection with transfer format '${E[e]}'.`), this._connectionState !== "Disconnected")
      return Promise.reject(new Error("Cannot start an HttpConnection that is not in the 'Disconnected' state."));
    if (this._connectionState = "Connecting", this._startInternalPromise = this._startInternal(e), await this._startInternalPromise, this._connectionState === "Disconnecting") {
      const o = "Failed to start the HttpConnection before stop() was called.";
      return this._logger.log(a.Error, o), await this._stopPromise, Promise.reject(new T(o));
    } else if (this._connectionState !== "Connected") {
      const o = "HttpConnection.startInternal completed gracefully but didn't enter the connection into the connected state!";
      return this._logger.log(a.Error, o), Promise.reject(new T(o));
    }
    this._connectionStarted = !0;
  }
  send(e) {
    return this._connectionState !== "Connected" ? Promise.reject(new Error("Cannot send data if the connection is not in the 'Connected' State.")) : (this._sendQueue || (this._sendQueue = new oe(this.transport)), this._sendQueue.send(e));
  }
  async stop(e) {
    if (this._connectionState === "Disconnected")
      return this._logger.log(a.Debug, `Call to HttpConnection.stop(${e}) ignored because the connection is already in the disconnected state.`), Promise.resolve();
    if (this._connectionState === "Disconnecting")
      return this._logger.log(a.Debug, `Call to HttpConnection.stop(${e}) ignored because the connection is already in the disconnecting state.`), this._stopPromise;
    this._connectionState = "Disconnecting", this._stopPromise = new Promise((o) => {
      this._stopPromiseResolver = o;
    }), await this._stopInternal(e), await this._stopPromise;
  }
  async _stopInternal(e) {
    this._stopError = e;
    try {
      await this._startInternalPromise;
    } catch {
    }
    if (this.transport) {
      try {
        await this.transport.stop();
      } catch (o) {
        this._logger.log(a.Error, `HttpConnection.transport.stop() threw error '${o}'.`), this._stopConnection();
      }
      this.transport = void 0;
    } else
      this._logger.log(a.Debug, "HttpConnection.transport is undefined in HttpConnection.stop() because start() failed.");
  }
  async _startInternal(e) {
    let o = this.baseUrl;
    this._accessTokenFactory = this._options.accessTokenFactory, this._httpClient._accessTokenFactory = this._accessTokenFactory;
    try {
      if (this._options.skipNegotiation)
        if (this._options.transport === S.WebSockets)
          this.transport = this._constructTransport(S.WebSockets), await this._startTransport(o, e);
        else
          throw new Error("Negotiation can only be skipped when using the WebSocket transport directly.");
      else {
        let r = null, s = 0;
        do {
          if (r = await this._getNegotiationResponse(o), this._connectionState === "Disconnecting" || this._connectionState === "Disconnected")
            throw new T("The connection was stopped during negotiation.");
          if (r.error)
            throw new Error(r.error);
          if (r.ProtocolVersion)
            throw new Error("Detected a connection attempt to an ASP.NET SignalR Server. This client only supports connecting to an ASP.NET Core SignalR Server. See https://aka.ms/signalr-core-differences for details.");
          if (r.url && (o = r.url), r.accessToken) {
            const i = r.accessToken;
            this._accessTokenFactory = () => i, this._httpClient._accessToken = i, this._httpClient._accessTokenFactory = void 0;
          }
          s++;
        } while (r.url && s < xe);
        if (s === xe && r.url)
          throw new Error("Negotiate redirection limit exceeded.");
        await this._createTransport(o, this._options.transport, r, e);
      }
      this.transport instanceof Pe && (this.features.inherentKeepAlive = !0), this._connectionState === "Connecting" && (this._logger.log(a.Debug, "The HttpConnection connected successfully."), this._connectionState = "Connected");
    } catch (r) {
      return this._logger.log(a.Error, "Failed to start the connection: " + r), this._connectionState = "Disconnected", this.transport = void 0, this._stopPromiseResolver(), Promise.reject(r);
    }
  }
  async _getNegotiationResponse(e) {
    const o = {}, [r, s] = V();
    o[r] = s;
    const i = this._resolveNegotiateUrl(e);
    this._logger.log(a.Debug, `Sending negotiation request: ${i}.`);
    try {
      const l = await this._httpClient.post(i, {
        content: "",
        headers: { ...o, ...this._options.headers },
        timeout: this._options.timeout,
        withCredentials: this._options.withCredentials
      });
      if (l.statusCode !== 200)
        return Promise.reject(new Error(`Unexpected status code returned from negotiate '${l.statusCode}'`));
      const c = JSON.parse(l.content);
      return (!c.negotiateVersion || c.negotiateVersion < 1) && (c.connectionToken = c.connectionId), c.useStatefulReconnect && this._options._useStatefulReconnect !== !0 ? Promise.reject(new ke("Client didn't negotiate Stateful Reconnect but the server did.")) : c;
    } catch (l) {
      let c = "Failed to complete negotiation with the server: " + l;
      return l instanceof A && l.statusCode === 404 && (c = c + " Either this is not a SignalR endpoint or there is a proxy blocking the connection."), this._logger.log(a.Error, c), Promise.reject(new ke(c));
    }
  }
  _createConnectUrl(e, o) {
    return o ? e + (e.indexOf("?") === -1 ? "?" : "&") + `id=${o}` : e;
  }
  async _createTransport(e, o, r, s) {
    let i = this._createConnectUrl(e, r.connectionToken);
    if (this._isITransport(o)) {
      this._logger.log(a.Debug, "Connection was provided an instance of ITransport, using that directly."), this.transport = o, await this._startTransport(i, s), this.connectionId = r.connectionId;
      return;
    }
    const l = [], c = r.availableTransports || [];
    let u = r;
    for (const d of c) {
      const f = this._resolveTransportOrError(d, o, s, u?.useStatefulReconnect === !0);
      if (f instanceof Error)
        l.push(`${d.transport} failed:`), l.push(f);
      else if (this._isITransport(f)) {
        if (this.transport = f, !u) {
          try {
            u = await this._getNegotiationResponse(e);
          } catch (p) {
            return Promise.reject(p);
          }
          i = this._createConnectUrl(e, u.connectionToken);
        }
        try {
          await this._startTransport(i, s), this.connectionId = u.connectionId;
          return;
        } catch (p) {
          if (this._logger.log(a.Error, `Failed to start the transport '${d.transport}': ${p}`), u = void 0, l.push(new ut(`${d.transport} failed: ${p}`, S[d.transport])), this._connectionState !== "Connecting") {
            const w = "Failed to select transport before stop() was called.";
            return this._logger.log(a.Debug, w), Promise.reject(new T(w));
          }
        }
      }
    }
    return l.length > 0 ? Promise.reject(new ht(`Unable to connect to the server with any of the available transports. ${l.join(" ")}`, l)) : Promise.reject(new Error("None of the transports supported by the client are supported by the server."));
  }
  _constructTransport(e) {
    switch (e) {
      case S.WebSockets:
        if (!this._options.WebSocket)
          throw new Error("'WebSocket' is not supported in your environment.");
        return new Ot(this._httpClient, this._accessTokenFactory, this._logger, this._options.logMessageContent, this._options.WebSocket, this._options.headers || {});
      case S.ServerSentEvents:
        if (!this._options.EventSource)
          throw new Error("'EventSource' is not supported in your environment.");
        return new Rt(this._httpClient, this._httpClient._accessToken, this._logger, this._options);
      case S.LongPolling:
        return new Pe(this._httpClient, this._logger, this._options);
      default:
        throw new Error(`Unknown transport: ${e}.`);
    }
  }
  _startTransport(e, o) {
    return this.transport.onreceive = this.onreceive, this.features.reconnect ? this.transport.onclose = async (r) => {
      let s = !1;
      if (this.features.reconnect)
        try {
          this.features.disconnected(), await this.transport.connect(e, o), await this.features.resend();
        } catch {
          s = !0;
        }
      else {
        this._stopConnection(r);
        return;
      }
      s && this._stopConnection(r);
    } : this.transport.onclose = (r) => this._stopConnection(r), this.transport.connect(e, o);
  }
  _resolveTransportOrError(e, o, r, s) {
    const i = S[e.transport];
    if (i == null)
      return this._logger.log(a.Debug, `Skipping transport '${e.transport}' because it is not supported by this client.`), new Error(`Skipping transport '${e.transport}' because it is not supported by this client.`);
    if (Vt(o, i))
      if (e.transferFormats.map((c) => E[c]).indexOf(r) >= 0) {
        if (i === S.WebSockets && !this._options.WebSocket || i === S.ServerSentEvents && !this._options.EventSource)
          return this._logger.log(a.Debug, `Skipping transport '${S[i]}' because it is not supported in your environment.'`), new at(`'${S[i]}' is not supported in your environment.`, i);
        this._logger.log(a.Debug, `Selecting transport '${S[i]}'.`);
        try {
          return this.features.reconnect = i === S.WebSockets ? s : void 0, this._constructTransport(i);
        } catch (c) {
          return c;
        }
      } else
        return this._logger.log(a.Debug, `Skipping transport '${S[i]}' because it does not support the requested transfer format '${E[r]}'.`), new Error(`'${S[i]}' does not support ${E[r]}.`);
    else
      return this._logger.log(a.Debug, `Skipping transport '${S[i]}' because it was disabled by the client.`), new ct(`'${S[i]}' is disabled by the client.`, i);
  }
  _isITransport(e) {
    return e && typeof e == "object" && "connect" in e;
  }
  _stopConnection(e) {
    if (this._logger.log(a.Debug, `HttpConnection.stopConnection(${e}) called while in state ${this._connectionState}.`), this.transport = void 0, e = this._stopError || e, this._stopError = void 0, this._connectionState === "Disconnected") {
      this._logger.log(a.Debug, `Call to HttpConnection.stopConnection(${e}) was ignored because the connection is already in the disconnected state.`);
      return;
    }
    if (this._connectionState === "Connecting")
      throw this._logger.log(a.Warning, `Call to HttpConnection.stopConnection(${e}) was ignored because the connection is still in the connecting state.`), new Error(`HttpConnection.stopConnection(${e}) was called while the connection is still in the connecting state.`);
    if (this._connectionState === "Disconnecting" && this._stopPromiseResolver(), e ? this._logger.log(a.Error, `Connection disconnected with error '${e}'.`) : this._logger.log(a.Information, "Connection disconnected."), this._sendQueue && (this._sendQueue.stop().catch((o) => {
      this._logger.log(a.Error, `TransportSendQueue.stop() threw error '${o}'.`);
    }), this._sendQueue = void 0), this.connectionId = void 0, this._connectionState = "Disconnected", this._connectionStarted) {
      this._connectionStarted = !1;
      try {
        this.onclose && this.onclose(e);
      } catch (o) {
        this._logger.log(a.Error, `HttpConnection.onclose(${e}) threw error '${o}'.`);
      }
    }
  }
  _resolveUrl(e) {
    if (e.lastIndexOf("https://", 0) === 0 || e.lastIndexOf("http://", 0) === 0)
      return e;
    if (!b.isBrowser)
      throw new Error(`Cannot resolve '${e}'.`);
    const o = window.document.createElement("a");
    return o.href = e, this._logger.log(a.Information, `Normalizing '${e}' to '${o.href}'.`), o.href;
  }
  _resolveNegotiateUrl(e) {
    const o = new URL(e);
    o.pathname.endsWith("/") ? o.pathname += "negotiate" : o.pathname += "/negotiate";
    const r = new URLSearchParams(o.searchParams);
    return r.has("negotiateVersion") || r.append("negotiateVersion", this._negotiateVersion.toString()), r.has("useStatefulReconnect") ? r.get("useStatefulReconnect") === "true" && (this._options._useStatefulReconnect = !0) : this._options._useStatefulReconnect === !0 && r.append("useStatefulReconnect", "true"), o.search = r.toString(), o.toString();
  }
}
function Vt(t, e) {
  return !t || (e & t) !== 0;
}
class oe {
  constructor(e) {
    this._transport = e, this._buffer = [], this._executing = !0, this._sendBufferedData = new F(), this._transportResult = new F(), this._sendLoopPromise = this._sendLoop();
  }
  send(e) {
    return this._bufferData(e), this._transportResult || (this._transportResult = new F()), this._transportResult.promise;
  }
  stop() {
    return this._executing = !1, this._sendBufferedData.resolve(), this._sendLoopPromise;
  }
  _bufferData(e) {
    if (this._buffer.length && typeof this._buffer[0] != typeof e)
      throw new Error(`Expected data to be of type ${typeof this._buffer} but was of type ${typeof e}`);
    this._buffer.push(e), this._sendBufferedData.resolve();
  }
  async _sendLoop() {
    for (; ; ) {
      if (await this._sendBufferedData.promise, !this._executing) {
        this._transportResult && this._transportResult.reject("Connection stopped.");
        break;
      }
      this._sendBufferedData = new F();
      const e = this._transportResult;
      this._transportResult = void 0;
      const o = typeof this._buffer[0] == "string" ? this._buffer.join("") : oe._concatBuffers(this._buffer);
      this._buffer.length = 0;
      try {
        await this._transport.send(o), e.resolve();
      } catch (r) {
        e.reject(r);
      }
    }
  }
  static _concatBuffers(e) {
    const o = e.map((i) => i.byteLength).reduce((i, l) => i + l), r = new Uint8Array(o);
    let s = 0;
    for (const i of e)
      r.set(new Uint8Array(i), s), s += i.byteLength;
    return r.buffer;
  }
}
class F {
  constructor() {
    this.promise = new Promise((e, o) => [this._resolver, this._rejecter] = [e, o]);
  }
  resolve() {
    this._resolver();
  }
  reject(e) {
    this._rejecter(e);
  }
}
const Mt = "json";
class Wt {
  constructor() {
    this.name = Mt, this.version = 2, this.transferFormat = E.Text;
  }
  /** Creates an array of {@link @microsoft/signalr.HubMessage} objects from the specified serialized representation.
   *
   * @param {string} input A string containing the serialized representation.
   * @param {ILogger} logger A logger that will be used to log messages that occur during parsing.
   */
  parseMessages(e, o) {
    if (typeof e != "string")
      throw new Error("Invalid input for JSON hub protocol. Expected a string.");
    if (!e)
      return [];
    o === null && (o = U.instance);
    const r = x.parse(e), s = [];
    for (const i of r) {
      const l = JSON.parse(i);
      if (typeof l.type != "number")
        throw new Error("Invalid payload.");
      switch (l.type) {
        case g.Invocation:
          this._isInvocationMessage(l);
          break;
        case g.StreamItem:
          this._isStreamItemMessage(l);
          break;
        case g.Completion:
          this._isCompletionMessage(l);
          break;
        case g.Ping:
          break;
        case g.Close:
          break;
        case g.Ack:
          this._isAckMessage(l);
          break;
        case g.Sequence:
          this._isSequenceMessage(l);
          break;
        default:
          o.log(a.Information, "Unknown message type '" + l.type + "' ignored.");
          continue;
      }
      s.push(l);
    }
    return s;
  }
  /** Writes the specified {@link @microsoft/signalr.HubMessage} to a string and returns it.
   *
   * @param {HubMessage} message The message to write.
   * @returns {string} A string containing the serialized representation of the message.
   */
  writeMessage(e) {
    return x.write(JSON.stringify(e));
  }
  _isInvocationMessage(e) {
    this._assertNotEmptyString(e.target, "Invalid payload for Invocation message."), e.invocationId !== void 0 && this._assertNotEmptyString(e.invocationId, "Invalid payload for Invocation message.");
  }
  _isStreamItemMessage(e) {
    if (this._assertNotEmptyString(e.invocationId, "Invalid payload for StreamItem message."), e.item === void 0)
      throw new Error("Invalid payload for StreamItem message.");
  }
  _isCompletionMessage(e) {
    if (e.result && e.error)
      throw new Error("Invalid payload for Completion message.");
    !e.result && e.error && this._assertNotEmptyString(e.error, "Invalid payload for Completion message."), this._assertNotEmptyString(e.invocationId, "Invalid payload for Completion message.");
  }
  _isAckMessage(e) {
    if (typeof e.sequenceId != "number")
      throw new Error("Invalid SequenceId for Ack message.");
  }
  _isSequenceMessage(e) {
    if (typeof e.sequenceId != "number")
      throw new Error("Invalid SequenceId for Sequence message.");
  }
  _assertNotEmptyString(e, o) {
    if (typeof e != "string" || e === "")
      throw new Error(o);
  }
}
const Lt = {
  trace: a.Trace,
  debug: a.Debug,
  info: a.Information,
  information: a.Information,
  warn: a.Warning,
  warning: a.Warning,
  error: a.Error,
  critical: a.Critical,
  none: a.None
};
function Bt(t) {
  const e = Lt[t.toLowerCase()];
  if (typeof e < "u")
    return e;
  throw new Error(`Unknown log level: ${t}`);
}
class Ht {
  configureLogging(e) {
    if (k.isRequired(e, "logging"), Ut(e))
      this.logger = e;
    else if (typeof e == "string") {
      const o = Bt(e);
      this.logger = new q(o);
    } else
      this.logger = new q(e);
    return this;
  }
  withUrl(e, o) {
    return k.isRequired(e, "url"), k.isNotEmpty(e, "url"), this.url = e, typeof o == "object" ? this.httpConnectionOptions = { ...this.httpConnectionOptions, ...o } : this.httpConnectionOptions = {
      ...this.httpConnectionOptions,
      transport: o
    }, this;
  }
  /** Configures the {@link @microsoft/signalr.HubConnection} to use the specified Hub Protocol.
   *
   * @param {IHubProtocol} protocol The {@link @microsoft/signalr.IHubProtocol} implementation to use.
   */
  withHubProtocol(e) {
    return k.isRequired(e, "protocol"), this.protocol = e, this;
  }
  withAutomaticReconnect(e) {
    if (this.reconnectPolicy)
      throw new Error("A reconnectPolicy has already been set.");
    return e ? Array.isArray(e) ? this.reconnectPolicy = new Ee(e) : this.reconnectPolicy = e : this.reconnectPolicy = new Ee(), this;
  }
  /** Configures {@link @microsoft/signalr.HubConnection.serverTimeoutInMilliseconds} for the {@link @microsoft/signalr.HubConnection}.
   *
   * @returns The {@link @microsoft/signalr.HubConnectionBuilder} instance, for chaining.
   */
  withServerTimeout(e) {
    return k.isRequired(e, "milliseconds"), this._serverTimeoutInMilliseconds = e, this;
  }
  /** Configures {@link @microsoft/signalr.HubConnection.keepAliveIntervalInMilliseconds} for the {@link @microsoft/signalr.HubConnection}.
   *
   * @returns The {@link @microsoft/signalr.HubConnectionBuilder} instance, for chaining.
   */
  withKeepAliveInterval(e) {
    return k.isRequired(e, "milliseconds"), this._keepAliveIntervalInMilliseconds = e, this;
  }
  /** Enables and configures options for the Stateful Reconnect feature.
   *
   * @returns The {@link @microsoft/signalr.HubConnectionBuilder} instance, for chaining.
   */
  withStatefulReconnect(e) {
    return this.httpConnectionOptions === void 0 && (this.httpConnectionOptions = {}), this.httpConnectionOptions._useStatefulReconnect = !0, this._statefulReconnectBufferSize = e?.bufferSize, this;
  }
  /** Creates a {@link @microsoft/signalr.HubConnection} from the configuration options specified in this builder.
   *
   * @returns {HubConnection} The configured {@link @microsoft/signalr.HubConnection}.
   */
  build() {
    const e = this.httpConnectionOptions || {};
    if (e.logger === void 0 && (e.logger = this.logger), !this.url)
      throw new Error("The 'HubConnectionBuilder.withUrl' method must be called before building the connection.");
    const o = new jt(this.url, e);
    return te.create(o, this.logger || U.instance, this.protocol || new Wt(), this.reconnectPolicy, this._serverTimeoutInMilliseconds, this._keepAliveIntervalInMilliseconds, this._statefulReconnectBufferSize);
  }
}
function Ut(t) {
  return t.log !== void 0;
}
var Y, Ie;
function zt() {
  return Ie || (Ie = 1, Y = mslxRequest), Y;
}
var Ft = zt();
const M = /* @__PURE__ */ Le(Ft), W = "/api/plugins/mslx-plugin-stun/tunnels", qt = () => M.get({ url: `${W}/list` }), Kt = (t) => M.post({ url: `${W}/create`, data: t }), Zt = (t) => M.post({ url: `${W}/update`, data: t }), Xt = (t) => M.post({ url: `${W}/delete?id=${t}` }), Jt = (t) => M.post({ url: `${W}/start?id=${t}` }), Gt = (t) => M.post({ url: `${W}/stop?id=${t}` }), Qt = /* @__PURE__ */ n.defineComponent({
  __name: "STUNPage",
  setup(t) {
    const e = n.ref(!1), o = n.ref([]), r = n.ref(!1), s = n.ref(!1), i = n.ref({
      name: "我的 STUN 隧道",
      localIp: "127.0.0.1",
      localPort: 25565,
      enableProxyProtocolV2: !1,
      maxConnections: 128
    }), l = n.ref(!1), c = n.ref(null), u = n.ref([]), d = n.ref(null), f = n.ref(null);
    let p = null;
    const w = (C) => {
      if (C < 1024) return `${C} B`;
      const v = C / 1024;
      if (v < 1024) return `${v.toFixed(2)} KB`;
      const I = v / 1024;
      return I < 1024 ? `${I.toFixed(2)} MB` : `${(I / 1024).toFixed(2)} GB`;
    }, P = async () => {
      await n.nextTick(), f.value && (f.value.scrollTop = f.value.scrollHeight);
    }, h = async () => {
      try {
        e.value = !0;
        const C = await qt();
        C && (o.value = C);
      } catch (C) {
        $.MessagePlugin.error(`拉取隧道列表失败: ${C.message}`);
      } finally {
        e.value = !1;
      }
    }, m = () => {
      s.value = !1, i.value = {
        name: "新建隧道",
        localIp: "127.0.0.1",
        localPort: 25565,
        enableProxyProtocolV2: !1,
        maxConnections: 128
      }, r.value = !0;
    }, Ne = (C) => {
      s.value = !0, i.value = { ...C }, r.value = !0;
    }, Ae = async () => {
      try {
        s.value ? (await Zt(i.value), $.MessagePlugin.success("隧道更新成功")) : (await Kt(i.value), $.MessagePlugin.success("隧道创建成功")), r.value = !1, h();
      } catch (C) {
        $.MessagePlugin.error(`操作失败: ${C.message}`);
      }
    }, De = async (C) => {
      const v = C.stats?.isRunning;
      try {
        v ? (await Gt(C.config.id), $.MessagePlugin.success("隧道已停止")) : (await Jt(C.config.id), $.MessagePlugin.success("隧道启动指令已下发")), h();
      } catch (I) {
        $.MessagePlugin.error(`操作失败: ${I.message}`);
      }
    }, Re = (C) => {
      const v = $.DialogPlugin.confirm({
        header: "删除确认",
        body: "确定要删除这个隧道吗？删除后配置不可恢复。",
        theme: "danger",
        onConfirm: async () => {
          try {
            await Xt(C), $.MessagePlugin.success("删除成功"), h();
          } catch (I) {
            $.MessagePlugin.error(`删除失败: ${I.message}`);
          } finally {
            v.destroy();
          }
        }
      });
    }, Oe = async (C) => {
      c.value = C, u.value = [], d.value = null, l.value = !0;
      try {
        p = new Ht().withUrl("/api/hubs/stun").withAutomaticReconnect().build(), p.on("ReceiveLog", (v) => {
          u.value.push(v), u.value.length > 500 && u.value.shift(), P();
        }), p.on("ReceiveStats", (v) => {
          d.value = v;
        }), await p.start(), await p.invoke("JoinGroup", C.id), u.value.push(`// MSLX 控制台：成功连接到隧道 [${C.name}] 日志流...`);
      } catch (v) {
        u.value.push(`// 连接控制台失败: ${v}`);
      }
    }, je = async () => {
      if (p && c.value)
        try {
          await p.invoke("LeaveGroup", c.value.id), await p.stop();
        } catch (C) {
          console.error("关闭 SignalR 失败", C);
        }
      p = null, c.value = null, l.value = !1;
    };
    return n.onMounted(() => {
      h();
    }), n.onBeforeUnmount(() => {
      p && p.stop();
    }), (C, v) => {
      const I = n.resolveComponent("t-tag"), L = n.resolveComponent("t-button"), Ve = n.resolveComponent("t-loading"), re = n.resolveComponent("t-input"), B = n.resolveComponent("t-form-item"), ne = n.resolveComponent("t-input-number"), Me = n.resolveComponent("t-switch"), We = n.resolveComponent("t-form"), se = n.resolveComponent("t-dialog");
      return n.openBlock(), n.createElementBlock("div", { class: "mx-auto flex flex-col gap-6 text-[var(--td-text-color-primary)]" }, [
        n.createElementVNode("div", { class: "design-card rounded-2xl glass-card border border-[var(--td-component-border)] shadow-sm p-6 flex flex-col md:flex-row justify-between items-center gap-4" }, [
          n.createElementVNode("div", { class: "flex items-center gap-4" }, [
            n.createElementVNode("div", { class: "w-14 h-14 shrink-0 flex items-center justify-center rounded-2xl bg-primary-light text-[var(--color-primary)]" }, [
              n.createVNode(n.unref(lt), { size: "28px" })
            ]),
            n.createElementVNode("div", null, [
              n.createElementVNode("h1", { class: "text-xl font-extrabold m-0 mb-1 flex items-center gap-2" }, [
                n.createTextVNode(" STUN 隧道管理 "),
                n.createVNode(I, {
                  theme: "success",
                  variant: "light-outline",
                  size: "small",
                  shape: "round"
                }, {
                  default: n.withCtx(() => [
                    n.createTextVNode("NAT1 穿透")
                  ]),
                  _: 1
                })
              ]),
              n.createElementVNode("p", { class: "text-[var(--td-text-color-secondary)] m-0 text-sm" }, " 基于 STUN 协议，突破内网限制，获取公网直连端口。 ")
            ])
          ]),
          n.createVNode(L, {
            theme: "primary",
            shape: "round",
            onClick: m
          }, {
            icon: n.withCtx(() => [
              n.createVNode(n.unref(Xe))
            ]),
            default: n.withCtx(() => [
              n.createTextVNode(" 新建隧道 ")
            ]),
            _: 1
          })
        ]),
        n.createVNode(Ve, {
          loading: e.value,
          text: "加载隧道列表中...",
          size: "small"
        }, {
          default: n.withCtx(() => [
            o.value.length === 0 ? (n.openBlock(), n.createElementBlock("div", {
              key: 0,
              class: "py-12 text-center text-[var(--td-text-color-secondary)]"
            }, " 暂无隧道配置，点击右上角新建。 ")) : n.createCommentVNode("", !0),
            n.createElementVNode("div", { class: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5" }, [
              (n.openBlock(!0), n.createElementBlock(n.Fragment, null, n.renderList(o.value, (_, Z) => (n.openBlock(), n.createElementBlock("div", {
                key: _.config.id,
                class: "list-item-anim design-card p-5 rounded-2xl glass-card border border-[var(--td-component-border)] flex flex-col gap-4 transition-all hover:-translate-y-1 hover:shadow-md",
                style: n.normalizeStyle({ animationDelay: `${Z * 0.1}s` })
              }, [
                n.createElementVNode("div", { class: "flex justify-between items-start border-b border-dashed border-[var(--td-border-level-2-color)] pb-3" }, [
                  n.createElementVNode("div", { class: "flex items-center gap-2" }, [
                    n.createElementVNode("div", {
                      class: n.normalizeClass(["w-2.5 h-2.5 rounded-full shadow-sm", _.stats?.isRunning ? "bg-[var(--color-success)] shadow-[var(--color-success)]" : "bg-[var(--td-text-color-placeholder)]"])
                    }, null, 2),
                    n.createElementVNode("h3", { class: "font-bold text-base m-0" }, n.toDisplayString(_.config.name), 1)
                  ]),
                  n.createElementVNode("div", { class: "flex gap-1" }, [
                    n.createVNode(L, {
                      shape: "square",
                      variant: "text",
                      size: "small",
                      onClick: (X) => Ne(_.config)
                    }, {
                      default: n.withCtx(() => [
                        n.createVNode(n.unref(Ye))
                      ]),
                      _: 1
                    }, 8, ["onClick"]),
                    n.createVNode(L, {
                      shape: "square",
                      variant: "text",
                      size: "small",
                      theme: "danger",
                      onClick: (X) => Re(_.config.id)
                    }, {
                      default: n.withCtx(() => [
                        n.createVNode(n.unref(Ge))
                      ]),
                      _: 1
                    }, 8, ["onClick"])
                  ])
                ]),
                n.createElementVNode("div", { class: "flex flex-col gap-2 text-sm text-[var(--td-text-color-secondary)] flex-grow" }, [
                  n.createElementVNode("div", { class: "flex justify-between" }, [
                    n.createElementVNode("span", null, "本地目标:"),
                    n.createElementVNode("span", { class: "font-mono text-[var(--td-text-color-primary)]" }, n.toDisplayString(_.config.localIp) + ":" + n.toDisplayString(_.config.localPort), 1)
                  ]),
                  n.createElementVNode("div", { class: "flex justify-between" }, [
                    n.createElementVNode("span", null, "公网地址:"),
                    n.createElementVNode("span", {
                      class: n.normalizeClass(["font-mono font-bold", _.stats?.isRunning ? "text-[var(--color-primary)]" : "text-[var(--td-text-color-placeholder)]"])
                    }, n.toDisplayString(_.stats?.outerAddress || "未分配"), 3)
                  ]),
                  n.createElementVNode("div", { class: "flex items-center gap-2 mt-1" }, [
                    _.config.enableProxyProtocolV2 ? (n.openBlock(), n.createBlock(I, {
                      key: 0,
                      theme: "warning",
                      variant: "light",
                      size: "small"
                    }, {
                      default: n.withCtx(() => [
                        n.createTextVNode("Proxy V2")
                      ]),
                      _: 1
                    })) : n.createCommentVNode("", !0),
                    n.createVNode(I, {
                      theme: "default",
                      variant: "light",
                      size: "small"
                    }, {
                      default: n.withCtx(() => [
                        n.createTextVNode("最大连接: " + n.toDisplayString(_.config.maxConnections), 1)
                      ]),
                      _: 2
                    }, 1024)
                  ])
                ]),
                n.createElementVNode("div", { class: "flex gap-3 pt-2" }, [
                  n.createVNode(L, {
                    class: "flex-1",
                    theme: _.stats?.isRunning ? "danger" : "primary",
                    variant: "outline",
                    onClick: (X) => De(_)
                  }, {
                    icon: n.withCtx(() => [
                      _.stats?.isRunning ? (n.openBlock(), n.createBlock(n.unref(rt), { key: 0 })) : (n.openBlock(), n.createBlock(n.unref(tt), { key: 1 }))
                    ]),
                    default: n.withCtx(() => [
                      n.createTextVNode(" " + n.toDisplayString(_.stats?.isRunning ? "停止" : "启动"), 1)
                    ]),
                    _: 2
                  }, 1032, ["theme", "onClick"]),
                  n.createVNode(L, {
                    class: "flex-1",
                    theme: "default",
                    onClick: (X) => Oe(_.config)
                  }, {
                    icon: n.withCtx(() => [
                      n.createVNode(n.unref(st))
                    ]),
                    default: n.withCtx(() => [
                      n.createTextVNode(" 控制台 ")
                    ]),
                    _: 1
                  }, 8, ["onClick"])
                ])
              ], 4))), 128))
            ])
          ]),
          _: 1
        }, 8, ["loading"]),
        n.createVNode(se, {
          visible: r.value,
          "onUpdate:visible": v[5] || (v[5] = (_) => r.value = _),
          header: s.value ? "编辑 STUN 隧道" : "新建 STUN 隧道",
          placement: "center",
          confirmBtn: s.value ? "保存修改" : "确认创建",
          onConfirm: Ae
        }, {
          default: n.withCtx(() => [
            n.createElementVNode("div", { class: "pt-4" }, [
              n.createVNode(We, {
                data: i.value,
                "label-width": "110px"
              }, {
                default: n.withCtx(() => [
                  n.createVNode(B, {
                    label: "隧道名称",
                    name: "name"
                  }, {
                    default: n.withCtx(() => [
                      n.createVNode(re, {
                        modelValue: i.value.name,
                        "onUpdate:modelValue": v[0] || (v[0] = (_) => i.value.name = _),
                        placeholder: "起个好记的名字"
                      }, null, 8, ["modelValue"])
                    ]),
                    _: 1
                  }),
                  n.createVNode(B, {
                    label: "本地 IP",
                    name: "localIp"
                  }, {
                    default: n.withCtx(() => [
                      n.createVNode(re, {
                        modelValue: i.value.localIp,
                        "onUpdate:modelValue": v[1] || (v[1] = (_) => i.value.localIp = _),
                        placeholder: "例如: 127.0.0.1"
                      }, null, 8, ["modelValue"])
                    ]),
                    _: 1
                  }),
                  n.createVNode(B, {
                    label: "本地端口",
                    name: "localPort"
                  }, {
                    default: n.withCtx(() => [
                      n.createVNode(ne, {
                        modelValue: i.value.localPort,
                        "onUpdate:modelValue": v[2] || (v[2] = (_) => i.value.localPort = _),
                        min: 1,
                        max: 65535,
                        class: "w-full"
                      }, null, 8, ["modelValue"])
                    ]),
                    _: 1
                  }),
                  n.createVNode(B, {
                    label: "并发连接数",
                    name: "maxConnections"
                  }, {
                    default: n.withCtx(() => [
                      n.createVNode(ne, {
                        modelValue: i.value.maxConnections,
                        "onUpdate:modelValue": v[3] || (v[3] = (_) => i.value.maxConnections = _),
                        min: 1,
                        max: 1024,
                        class: "w-full"
                      }, null, 8, ["modelValue"])
                    ]),
                    _: 1
                  }),
                  n.createVNode(B, {
                    label: "Proxy V2 协议",
                    name: "enableProxyProtocolV2"
                  }, {
                    default: n.withCtx(() => [
                      n.createVNode(Me, {
                        modelValue: i.value.enableProxyProtocolV2,
                        "onUpdate:modelValue": v[4] || (v[4] = (_) => i.value.enableProxyProtocolV2 = _)
                      }, null, 8, ["modelValue"]),
                      n.createElementVNode("div", { class: "ml-3 text-xs text-[var(--td-text-color-secondary)]" }, "向后端传递真实玩家 IP (需服务端支持)")
                    ]),
                    _: 1
                  })
                ]),
                _: 1
              }, 8, ["data"])
            ])
          ]),
          _: 1
        }, 8, ["visible", "header", "confirmBtn"]),
        n.createVNode(se, {
          visible: l.value,
          "onUpdate:visible": v[6] || (v[6] = (_) => l.value = _),
          header: `终端 - ${c.value?.name || "未知"}`,
          placement: "center",
          width: "800px",
          footer: !1,
          onClose: je
        }, {
          default: n.withCtx(() => [
            n.createElementVNode("div", { class: "flex flex-col h-[500px] mt-2" }, [
              n.createElementVNode("div", { class: "grid grid-cols-4 gap-3 mb-4" }, [
                n.createElementVNode("div", { class: "bg-secondary-light p-3 rounded-xl border border-[var(--td-component-border)]" }, [
                  n.createElementVNode("div", { class: "text-xs text-[var(--td-text-color-secondary)] mb-1" }, "上行速度"),
                  n.createElementVNode("div", { class: "font-mono font-bold text-[var(--color-primary)]" }, n.toDisplayString(d.value ? w(d.value.speedUpload) : "0 B") + "/s", 1)
                ]),
                n.createElementVNode("div", { class: "bg-secondary-light p-3 rounded-xl border border-[var(--td-component-border)]" }, [
                  n.createElementVNode("div", { class: "text-xs text-[var(--td-text-color-secondary)] mb-1" }, "下行速度"),
                  n.createElementVNode("div", { class: "font-mono font-bold text-[var(--color-success)]" }, n.toDisplayString(d.value ? w(d.value.speedDownload) : "0 B") + "/s", 1)
                ]),
                n.createElementVNode("div", { class: "bg-secondary-light p-3 rounded-xl border border-[var(--td-component-border)]" }, [
                  n.createElementVNode("div", { class: "text-xs text-[var(--td-text-color-secondary)] mb-1" }, "当前连接数"),
                  n.createElementVNode("div", { class: "font-mono font-bold text-[var(--color-warning)]" }, n.toDisplayString(d.value?.activeConnections || 0) + " / " + n.toDisplayString(c.value?.maxConnections || 128), 1)
                ]),
                n.createElementVNode("div", { class: "bg-secondary-light p-3 rounded-xl border border-[var(--td-component-border)]" }, [
                  n.createElementVNode("div", { class: "text-xs text-[var(--td-text-color-secondary)] mb-1" }, "总流量"),
                  n.createElementVNode("div", { class: "font-mono font-bold text-[var(--td-text-color-primary)]" }, n.toDisplayString(d.value ? w(d.value.totalUpload + d.value.totalDownload) : "0 B"), 1)
                ])
              ]),
              n.createElementVNode("div", { class: "relative w-full flex-grow rounded-xl border border-[var(--td-component-border)] bg-[#1e1e1e] overflow-hidden shadow-inner flex flex-col" }, [
                n.createElementVNode("div", { class: "flex items-center px-4 py-2.5 bg-[#2d2d2d] border-b border-[#404040]" }, [
                  n.createElementVNode("div", { class: "flex gap-1.5" }, [
                    n.createElementVNode("div", { class: "w-3 h-3 rounded-full bg-[#ff5f56]" }),
                    n.createElementVNode("div", { class: "w-3 h-3 rounded-full bg-[#ffbd2e]" }),
                    n.createElementVNode("div", { class: "w-3 h-3 rounded-full bg-[#27c93f]" })
                  ]),
                  n.createElementVNode("span", { class: "ml-4 text-xs font-mono text-[#858585]" }, "mslx-stun-daemon"),
                  d.value?.isRunning ? (n.openBlock(), n.createElementBlock("span", {
                    key: 0,
                    class: "ml-auto text-xs text-[var(--color-success)] flex items-center gap-1"
                  }, [
                    n.createElementVNode("div", { class: "w-2 h-2 rounded-full bg-[var(--color-success)] animate-pulse" }),
                    n.createTextVNode(" 运行中 ")
                  ])) : n.createCommentVNode("", !0)
                ]),
                n.createElementVNode("div", {
                  ref_key: "logContainerRef",
                  ref: f,
                  class: "p-4 overflow-y-auto h-full flex-grow custom-scrollbar"
                }, [
                  n.createElementVNode("pre", { class: "m-0 text-[13px] font-mono leading-relaxed text-[#d4d4d4] whitespace-pre-wrap" }, [
                    (n.openBlock(!0), n.createElementBlock(n.Fragment, null, n.renderList(u.value, (_, Z) => (n.openBlock(), n.createElementBlock("code", {
                      key: Z,
                      class: "block"
                    }, n.toDisplayString(_), 1))), 128))
                  ]),
                  u.value.length === 0 ? (n.openBlock(), n.createElementBlock("div", {
                    key: 0,
                    class: "text-[#56b6c2] text-sm font-mono mt-2"
                  }, " > 等待日志输出... ")) : n.createCommentVNode("", !0)
                ], 512)
              ])
            ])
          ]),
          _: 1
        }, 8, ["visible", "header"])
      ]);
    };
  }
}), Yt = (t, e) => {
  const o = t.__vccOpts || t;
  for (const [r, s] of e)
    o[r] = s;
  return o;
}, eo = /* @__PURE__ */ Yt(Qt, [["__scopeId", "data-v-1bc18c22"]]), oo = {
  name: "STunPlugin",
  version: "1.0.0",
  // 注入路由
  routes: [
    // 插入在原有的一级菜单内
    {
      parentName: "frp",
      path: "stun",
      name: "STUNPage",
      component: eo,
      meta: { title: "STUN 隧道", icon: "internet", roleCode: ["admin"] }
    }
  ],
  // 注入组件
  extensions: []
};
export {
  oo as pluginConfig
};
